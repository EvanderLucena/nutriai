## Resumo
Corrige os bugs 11 e 12 do painel de bugs:

- **Bug 11:** Botão inferior esquerdo para trocar tema não funcionava. Movido o toggle de tema do Topbar (superior direito) para o Rail (botão de Ajustes, inferior esquerdo). Agora o botão de Ajustes alterna entre modo claro/escuro.
- **Bug 12:** Data/hora no painel superior direito estavam mockados. Substituído por horário real via `useEffect` + `setInterval(60s)`, formatado em pt-BR.

## Mudanças
- `Topbar.tsx`: remove botão de tema; clock agora usa `new Date()` atualizado a cada minuto
- `Rail.tsx`: botão "Ajustes" agora chama `toggleTheme()`; adicionado import do `useThemeStore`
- `navigationStore.ts`: exporta `ViewType` para tipagem do Rail

## Checklist
- [x] `npx tsc --noEmit` passou
- [x] `npx vitest run` passou (95 tests)
