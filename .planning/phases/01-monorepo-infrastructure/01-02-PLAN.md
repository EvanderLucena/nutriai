---
phase: 01-monorepo-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/vite.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/vite-env.d.ts
  - frontend/src/types/index.ts
  - frontend/src/api/client.ts
  - frontend/src/api/types.ts
  - frontend/src/hooks/useTheme.ts
  - frontend/src/lib/utils.ts
  - frontend/src/components/ui/Button.tsx
  - frontend/src/components/ui/Input.tsx
  - frontend/src/components/ui/Card.tsx
  - frontend/src/components/ui/Modal.tsx
  - frontend/src/components/shell/Rail.tsx
  - frontend/src/components/shell/Sidebar.tsx
  - frontend/src/components/shell/Topbar.tsx
  - frontend/src/views/HomeView.tsx
  - frontend/src/styles/globals.css
  - frontend/tailwind.config.ts
  - frontend/postcss.config.js
  - frontend/.eslintrc.cjs
  - frontend/.prettierrc
autonomous: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "Frontend Vite dev server starts with npm run dev and serves on port 5173"
    - "Tailwind custom theme tokens match prototype CSS custom properties (--lime, --coral, --sage, --amber, --sky)"
    - "Light and dark themes toggle correctly via data-theme attribute on html element"
    - "Health endpoint is callable from frontend Axios client (CORS configured)"
  artifacts:
    - path: "frontend/package.json"
      provides: "Project dependencies and scripts"
    - path: "frontend/src/styles/globals.css"
      provides: "Tailwind directives + all CSS custom properties from prototype"
    - path: "frontend/tailwind.config.ts"
      provides: "Custom theme mapping prototype design tokens to Tailwind utilities"
    - path: "frontend/src/api/client.ts"
      provides: "Axios instance with base URL and interceptor setup"
      exports: ["apiClient"]
    - path: "frontend/src/components/ui/Button.tsx"
      provides: "Button component matching prototype design (primary, secondary, ghost, ai variants)"
    - path: "frontend/src/components/ui/Modal.tsx"
      provides: "Modal component with portal rendering"
    - path: "frontend/src/hooks/useTheme.ts"
      provides: "Theme toggle hook for light/dark mode switching"
  key_links:
    - from: "frontend/src/api/client.ts"
      to: "http://localhost:8080/api/v1"
      via: "Axios baseURL pointing to backend"
      pattern: "baseURL.*api/v1"
    - from: "frontend/src/styles/globals.css"
      to: "frontend/tailwind.config.ts"
      via: "CSS custom properties mapped to Tailwind theme extensions"
      pattern: "colors.*var\\(--"
    - from: "frontend/src/hooks/useTheme.ts"
      to: "frontend/src/styles/globals.css"
      via: "Sets data-theme attribute on document element"
      pattern: "data-theme"
---

<objective>
Initialize the frontend project with Vite + React + TypeScript + Tailwind, including custom theme tokens that match the prototype's design system, base component library, Axios client, and theme toggle.

Purpose: This plan creates the frontend foundation that Phase 2 (Frontend Migration) will build upon. The custom Tailwind theme ensures that when prototype components are migrated, their visual identity is preserved identically. The component library and app shell provide the scaffolding for all authenticated views.

Output: A working Vite dev server with HMR, Tailwind configured with all prototype design tokens, 4 base UI components (Button, Input, Card, Modal), light/dark theme toggle, and an Axios client ready for API calls.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-infrastructure/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md

<interfaces>
<!-- Prototype CSS custom properties to be mapped (from styles.css :root and [data-theme] selectors) -->

<!-- Source: styles.css lines 2-38 — colors and tokens -->
CSS Custom Properties to preserve in globals.css:
  --ink: #0B0C0A
  --ink-2: #1A1D17
  --ink-3: #2A2E26
  --paper: #FEFCF6
  --paper-2: #F4F1EA
  --paper-3: #E8E4D8
  --lime: #D4FF4F
  --lime-dim: #9CBF2B
  --coral: #FF6B4A
  --coral-dim: #CC553B
  --sage: #7FB77E
  --sage-dim: #5A8A5A
  --amber: #E8B84A
  --sky: #7AB7D9
  --font-ui: 'Inter Tight'
  --font-mono: 'JetBrains Mono'
  --font-serif: 'Instrument Serif'
  --radius-sm: 3px
  --radius: 6px
  --radius-lg: 10px

Light theme (data-theme="light"):
  --bg: var(--paper)
  --surface: #FEFCF6
  --surface-2: var(--paper-2)
  --border: #D8D2C1
  --border-2: #C5BEA9
  --fg: #0B0C0A
  --fg-muted: #6B6F62
  --fg-subtle: #9A9C8E
  --ink-contrast: #0B0C0A

Dark theme (data-theme="dark"):
  --bg: #0B0C0A
  --surface: #121410
  --surface-2: #1A1D17
  --border: #2A2E26
  --border-2: #3A3F35
  --fg: #F4F1EA
  --fg-muted: #9A9C8E
  --fg-subtle: #6B6F62
  --ink-contrast: #F4F1EA

<!-- Health endpoint from Plan 01 -->
GET /api/v1/health → { status: "UP", timestamp, version: "0.1.0", db: "connected" }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: initialize Vite + React + TypeScript project with Tailwind and routing</name>
  <files>
    frontend/package.json, frontend/tsconfig.json, frontend/tsconfig.app.json,
    frontend/tsconfig.node.json, frontend/vite.config.ts, frontend/index.html,
    frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/vite-env.d.ts,
    frontend/src/types/index.ts, frontend/src/api/client.ts, frontend/src/api/types.ts,
    frontend/src/hooks/useTheme.ts, frontend/src/styles/globals.css,
    frontend/tailwind.config.ts, frontend/postcss.config.js
  </files>
  <action>
    Initialize the frontend project in the /frontend directory per D-11 (flat-by-type structure), D-12 (Context + Zustand), D-13 (React Router v7), D-14 (Axios), and D-15 (custom Tailwind theme tokens).

    **Use `npm create vite@latest frontend -- --template react-ts`** from the project root, then customize. Or scaffold manually — either way, the result must be a working Vite + React 19 + TypeScript 5.7+ project.

    **package.json dependencies** (per research STACK.md):
    - react: ^19.0.0, react-dom: ^19.0.0
    - react-router: ^7.6.0
    - zustand: ^5.0.0
    - axios: ^1.7.0
    - clsx: ^2.1.0
    - tailwind-merge: ^3.0.0
    Dev dependencies:
    - typescript: ^5.7.0
    - vite: ^6.2.0
    - @vitejs/plugin-react: ^4.4.0
    - tailwindcss: ^4.1.0 (with @tailwindcss/vite plugin)
    - vitest: ^3.1.0
    - @testing-library/react: ^16.0.0
    - @testing-library/jest-dom: ^6.6.0
    - eslint, prettier (with minimal config)

    **Folder structure** per D-11:
    ```
    frontend/src/
    ├── api/
    │   ├── client.ts          # Axios instance
    │   └── types.ts           # API response types
    ├── components/
    │   ├── ui/                # Base components (task 2)
    │   └── shell/             # Navigation chrome (task 2)
    ├── hooks/
    │   └── useTheme.ts
    ├── lib/
    │   └── utils.ts           # cn() helper (clsx + tailwind-merge)
    ├── views/
    │   └── HomeView.tsx       # Placeholder home view
    ├── types/
    │   └── index.ts
    ├── styles/
    │   └── globals.css
    ├── App.tsx
    └── main.tsx
    ```

    **tailwind.config.ts** — Per D-15, map ALL prototype CSS custom properties to Tailwind theme extensions. Use CSS-first config (Tailwind v4 style) within `globals.css` `@theme` directive OR a `tailwind.config.ts` file — whichever matches the Tailwind 4 approach. The key requirement is that `bg-ink`, `text-lime`, `border-sage` etc. all work as Tailwind utilities referencing the CSS custom properties.

    **globals.css** — Per D-15 and CONVENTIONS.md, copy the prototype's `:root` CSS custom properties and `[data-theme="light"]` / `[data-theme="dark"]` overrides. This is the single source of truth for all design tokens. Include:
    1. Tailwind v4 directives (`@import "tailwindcss"` or `@tailwind` directives depending on version)
    2. All CSS custom properties from lines 2-38 of the prototype's styles.css
    3. All `[data-theme]` theme overrides (dark/light)
    4. Base resets (box-sizing, body, scrollbar)
    5. Typography classes (`.mono`, `.serif`, `.eyebrow`, `.tnum`)

    **vite.config.ts** — Configure proxy for /api to backend:8080 in dev mode:
    ```typescript
    server: {
      port: 5173,
      proxy: {
        '/api': {
          target: 'http://localhost:8080',
          changeOrigin: true,
        },
      },
    },
    ```

    **src/api/client.ts** — Per D-14: Axios instance with `baseURL: '/api/v1'`, request interceptor for auth token (placeholder — will add real token logic in Phase 3), and response interceptor for error handling (map ApiResponse error shape to rejected promises).

    **src/api/types.ts** — TypeScript types matching the backend ApiResponse shape:
    ```typescript
    export interface ApiResponse<T> {
      success: boolean;
      data: T | null;
      errors?: string[];
      message?: string;
    }
    export interface HealthResponse {
      status: string;
      timestamp: string;
      version: string;
      db: string;
    }
    ```

    **src/hooks/useTheme.ts** — Per D-15, a Zustand store for theme state that:
    - Reads initial theme from localStorage or system preference
    - Toggles between "light" and "dark"
    - Sets `data-theme` attribute on `document.documentElement`
    - Persists theme choice to localStorage

    **src/lib/utils.ts** — `cn()` helper combining clsx and tailwind-merge:
    ```typescript
    import { clsx, type ClassValue } from 'clsx';
    import { twMerge } from 'tailwind-merge';
    export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
    ```

    **src/App.tsx** — React Router v7 setup per D-13 with route definitions:
    - `/` → HomeView (authenticated placeholder)
    - Uses createBrowserRouter + RouterProvider pattern
    - AuthProvider placeholder (Zustand store for auth state per D-12)
    - Sets up theme using useTheme hook

    **src/types/index.ts** — Export all shared types from this barrel file.

    **.eslintrc.cjs** — Minimal ESLint config for React + TypeScript.
    **.prettierrc** — Prettier config matching the project style.

    **Important:** Do NOT use Babel standalone or CDN imports. This is a proper Vite + TypeScript project with ES module imports. No `window` globals, no `<script type="text/babel">`.
  </action>
  <verify>
    <automated>cd frontend && npm run build && echo "BUILD_PASS" || echo "BUILD_FAIL"</automated>
  </verify>
  <done>
    - Vite dev server starts with `npm run dev` on port 5173
    - `npm run build` produces a production bundle with zero errors
    - Tailwind custom theme tokens work (bg-lime, text-ink, border-sage, etc.)
    - CSS custom properties for light/dark themes are in globals.css
    - Axios client configured with /api/v1 base URL
    - React Router set up with createBrowserRouter
    - Zustand theme store toggles data-theme attribute
    - cn() utility function works for class composition
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>task 2: create base component library and app shell</name>
  <files>
    frontend/src/components/ui/Button.tsx, frontend/src/components/ui/Input.tsx,
    frontend/src/components/ui/Card.tsx, frontend/src/components/ui/Modal.tsx,
    frontend/src/components/shell/Rail.tsx, frontend/src/components/shell/Sidebar.tsx,
    frontend/src/components/shell/Topbar.tsx, frontend/src/views/HomeView.tsx
  </files>
  <action>
    Per D-16, create 4 base UI components ported from prototype design, and 3 shell components for the authenticated layout.

    **Button.tsx** — Based on prototype's `.btn`, `.btn-primary`, `.btn-secondary`, `.btn-ghost`, `.btn-ai` classes (from CONVENTIONS.md and styles.css lines 334-496). Props:
    - `variant`: 'primary' | 'secondary' | 'ghost' | 'ai' | 'danger'
    - `size`: 'sm' | 'md' | 'lg'
    - `children`, `onClick`, `disabled`, `className`, `type` (button/submit)
    - Style uses Tailwind custom token utilities (bg-lime for primary, bg-coral for danger, etc.)
    - `.btn-ai` variant uses the lime accent with sparkle effect

    **Input.tsx** — Based on prototype's `.auth-input` and `.search` patterns. Props:
    - `label`, `placeholder`, `value`, `onChange`, `type`, `error`, `className`
    - Supports error state with coral border
    - Uses Inter Tight font (--font-ui)

    **Card.tsx** — Based on prototype's `.card`, `.card-h`, `.card-b` pattern. Props:
    - `title?: string` — renders card header
    - `children` — renders card body
    - `className` — for additional styling
    - Uses `var(--surface)` background, `var(--border)` border

    **Modal.tsx** — Based on prototype's portal-based dropdown pattern (uses `createPortal` to `document.body`). Props:
    - `open: boolean`
    - `onClose: () => void`
    - `title?: string`
    - `children`
    - Backdrop click closes, Escape key closes
    - Focus trap not needed yet (add Radix in Phase 2)

    **Rail.tsx** — Left icon sidebar per prototype's `.rail` pattern (56px wide). Placeholder icons for: Home, Users/Patients, Plan, Insights. Uses IconHome pattern from prototype. The Rail is a vertical bar with icon-only navigation items. Active item gets `var(--lime)` accent.

    **Sidebar.tsx** — Patient list sidebar per prototype's `.sidebar` pattern (288px wide). Contains:
    - Search input at top
    - Patient list (placeholder items — will be real data in Phase 4)
    - Status filter chips (All, On Track, Warning, Danger using `var(--sage)`, `var(--amber)`, `var(--coral)`)

    **Topbar.tsx** — Breadcrumb header per prototype's `.topbar` pattern. Shows current view name and a theme toggle button (light/dark). Uses `useTheme` hook.

    **HomeView.tsx** — Placeholder view that shows:
    - A title "Dashboard" (using serif font)
    - A card with the health endpoint response (fetching from /api/v1/health via the Axios client)
    - Theme toggle button

    **Important styling note:** Use Tailwind utilities with custom theme tokens (bg-ink, text-lime, bg-sage, etc.) for all component styling. The goal is NOT to replicate every prototype detail — it's to ensure the Tailwind tokens map correctly to the prototype's color system and that the basic component shapes exist. Visual fidelity comes in Phase 2.

    All text should be in pt-BR per project constraint "Idioma: Tudo em pt-BR".
  </action>
  <verify>
    <automated>cd frontend && npm run build && echo "BUILD_PASS" || echo "BUILD_FAIL"</automated>
  </verify>
  <done>
    - Button component renders primary, secondary, ghost, ai, and danger variants with correct prototype colors
    - Input component renders with label, placeholder, and error state
    - Card component renders with optional title header and body
    - Modal component opens/closes with portal rendering to document.body
    - Rail renders 56px sidebar with icon navigation
    - Sidebar renders 288px panel with search and filter chips
    - Topbar renders with theme toggle that switches light/dark
    - HomeView fetches and displays health endpoint data
    - Theme toggle persists choice to localStorage and sets data-theme attribute
    - All components use Tailwind custom token utilities (not hardcoded colors)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → API proxy | Requests from Vite dev server to backend via proxy (dev) or direct (prod) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | X (Elevation) | Axios interceptor | accept | Auth token placeholder in Phase 1; real auth added Phase 3 |
| T-01-07 | I (Info Disclosure) | Source maps in production | mitigate | Vite config disables source maps in production build |
</threat_model>

<verification>
1. Run `npm run build` in frontend directory — build succeeds with zero errors
2. Run `npm run dev` in frontend — Vite dev server starts on port 5173
3. Verify Tailwind classes using custom tokens compile (bg-lime, text-ink, bg-sage, etc.)
4. Verify globals.css contains all CSS custom properties from prototype
5. Verify data-theme="dark" class switching works on html element
6. Verify Axios client has baseURL '/api/v1' configured
7. Verify React Router renders HomeView at / path
8. Verify no Babel standalone or CDN imports exist
9. Verify all UI components (Button, Input, Card, Modal) render without TypeScript errors
</verification>

<success_criteria>
- Frontend builds and serves with Vite dev server on port 5173
- Tailwind custom theme tokens match all prototype CSS custom properties
- Light/dark theme toggle works via data-theme attribute
- 4 base UI components render with prototype design tokens
- Shell components (Rail, Sidebar, Topbar) render with correct dimensions and placeholders
- Axios client configured for /api/v1 with interceptor skeleton
- React Router v7 configured with HomeView at /
- TypeScript compiles with zero errors
- No Babel standalone, CDN imports, or window globals
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-infrastructure/01-02-SUMMARY.md`
</output>