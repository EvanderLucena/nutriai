---
phase: 02-frontend-migration
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - frontend/src/views/HomeView.tsx
  - frontend/src/components/KPI.tsx
  - frontend/src/views/PatientsView.tsx
  - frontend/src/components/patients/Avatar.tsx
  - frontend/src/components/patients/Pagination.tsx
  - frontend/src/components/patients/PatientTable.tsx
  - frontend/src/components/patients/PatientGrid.tsx
  - frontend/src/components/patients/NewPatientModal.tsx
  - frontend/src/components/patients/EditPatientModal.tsx
  - frontend/src/components/patients/index.ts
autonomous: true
requirements:
  - INFRA-02
must_haves:
  truths:
    - "Dashboard home renders KPIs (active patients, adherence metrics) and patient grid with real mock data"
    - "Patient list shows all 12 mock patients with status filters and search"
    - "Table/Grid toggle switches between table and card layouts"
    - "Pagination controls work for patient list with correct page size"
    - "New Patient and Edit Patient modals open and close with form fields"
    - "Avatar component shows status dot (on track=green, warning=amber, danger=red)"
  artifacts:
    - path: "frontend/src/views/HomeView.tsx"
      provides: "Dashboard with KPI cards and patient grid"
      min_lines: 80
    - path: "frontend/src/components/KPI.tsx"
      provides: "KPI metric card component"
      exports: ["KPI"]
    - path: "frontend/src/views/PatientsView.tsx"
      provides: "Patient list with filter, search, table/grid toggle"
      min_lines: 200
    - path: "frontend/src/components/patients/Avatar.tsx"
      provides: "Patient avatar with status dot"
      exports: ["Avatar"]
    - path: "frontend/src/components/patients/Pagination.tsx"
      provides: "Pagination controls"
      exports: ["Pagination"]
  key_links:
    - from: "frontend/src/views/HomeView.tsx"
      to: "frontend/src/data/patients.ts"
      via: "import PATIENTS for KPI data"
      pattern: "import.*PATIENTS"
    - from: "frontend/src/views/PatientsView.tsx"
      to: "frontend/src/data/patients.ts"
      via: "import PATIENTS for patient list"
      pattern: "import.*PATIENTS"
    - from: "frontend/src/views/PatientsView.tsx"
      to: "frontend/src/stores/navigationStore.ts"
      via: "useNavigationStore for setActivePatientId"
      pattern: "useNavigationStore"

---

<objective>
Migrate the dashboard home view and patient list views — the core daily-use screens for nutritionists. This includes KPI cards, patient table/grid, status filters, search, pagination, and patient modals.

Purpose: The home screen and patient list are the most frequently used views. They demonstrate that the data layer (Plan 01), layout shell (Plan 02), and theme system all work together correctly.
Output: HomeView with KPIs and patient cards, PatientsView with full table/grid/filter/search/modal functionality.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@view_home.jsx — HomeView (104 lines) + KPI component
@view_patients.jsx — PatientsView (554 lines) with PatientTable, PatientGrid, Avatar, Pagination, NewPatientModal, EditPatientModal, filters, search
@frontend/src/data/patients.ts — PATIENTS mock data (Plan 01)
@frontend/src/data/aggregate.ts — AGGREGATE mock data (Plan 01)
@frontend/src/data/ana.ts — ANA detailed patient data (Plan 01)
@frontend/src/components/viz/ — Ring, Sparkline, MacroRings (Plan 01)
@frontend/src/components/icons/index.tsx — All icons (Plan 01)
@frontend/src/components/ui/ — Button, Input, Card, Modal (Phase 1)
@frontend/src/stores/navigationStore.ts — View and patient navigation (Plan 02)

<interfaces>
From frontend/src/data/patients.ts:
```typescript
export const PATIENTS: Patient[];
```

From frontend/src/data/aggregate.ts:
```typescript
export const AGGREGATE: { activePatients: number; adherence: number; ... };
```

From frontend/src/types/patient.ts:
```typescript
export type PatientStatus = 'ontrack' | 'warning' | 'danger';
export interface Patient { id: string; name: string; status: PatientStatus; ... }
```

From frontend/src/stores/navigationStore.ts:
```typescript
export const useNavigationStore: UseBoundStore<StoreApi<NavigationState>>;
// NavigationState: { activeView, activePatientId, sidebarOpen, statusFilter, ... }
```

From frontend/src/components/ui/Modal.tsx:
```typescript
export function Modal({ open, onClose, title, children, className }: ModalProps): ReactElement
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Migrate HomeView with KPIs</name>
  <files>frontend/src/views/HomeView.tsx, frontend/src/components/KPI.tsx</files>
  <read_first>
    - view_home.jsx (HomeView + KPI — 104 lines with metric cards and patient grid)
    - data.jsx (PATIENTS array and AGGREGATE object for KPI values)
    - frontend/src/data/patients.ts (Plan 01 — typed PATIENTS data)
    - frontend/src/data/aggregate.ts (Plan 01 — typed AGGREGATE data)
    - frontend/src/components/viz/Ring.tsx (Plan 01 — for adherence ring)
    - frontend/src/components/viz/Sparkline.tsx (Plan 01 — for trend lines)
  </read_first>
  <action>
    1. **Migrate `KPI.tsx`** component from `view_home.jsx` KPI function:
       - Props: `{ label: string; value: string | number; change?: string; trend?: 'up' | 'down'; icon?: React.ReactNode; className?: string }`
       - Renders metric card with:
         - `.eyebrow` label (10.5px, uppercase, tracking)
         - Large `.mono .tnum` value
         - Optional change indicator with colored arrow (sage for up, coral for down)
         - Optional icon
       - Use Tailwind classes: `bg-surface rounded-[var(--radius-lg)] border border-border p-4`
       - Value displayed with `.mono .tnum` font classes for tabular number alignment

    2. **Migrate `HomeView.tsx`** from `view_home.jsx`:
       - Dashboard greeting with nutritionist name placeholder
       - 4 KPI cards in a grid layout:
         - "Pacientes ativos" — count from PATIENTS
         - "Adesão média" — percentage with Ring visualization
         - "Refeições registradas" — count with trend Sparkline
         - "Consultas este mês" — count
       - Patient grid below KPIs showing patient cards with:
         - Avatar with status dot (green/amber/red per PatientStatus)
         - Patient name, status label, objective
         - Click navigates to patient detail via `useNavigationStore().setActivePatientId(id)` + `setView('patient')`
       - Use `PATIENTS` from `@/data/patients` for data
       - Use `AGGREGATE` from `@/data/aggregate` for dashboard stats
       - Responsive: 4-column KPI grid on desktop, 2-column on tablet, 1-column on mobile
       - All text in pt-BR matching prototype exactly
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && echo "HomeView compiles successfully"</automated>
  </verify>
  <done>HomeView renders dashboard with 4 KPI cards and patient grid. KPI component reusable for any metric. All values from mock data. Patient cards navigate to detail view. pt-BR text throughout.</done>
</task>

<task type="auto">
  <name>task 2: Migrate PatientsView with table, grid, filters, search, modals</name>
  <files>frontend/src/views/PatientsView.tsx, frontend/src/components/patients/Avatar.tsx, frontend/src/components/patients/Pagination.tsx, frontend/src/components/patients/PatientTable.tsx, frontend/src/components/patients/PatientGrid.tsx, frontend/src/components/patients/NewPatientModal.tsx, frontend/src/components/patients/EditPatientModal.tsx, frontend/src/components/patients/index.ts</files>
  <read_first>
    - view_patients.jsx (554 lines — full patient list with table/grid, filters, search, pagination, modals, Avatar component)
    - frontend/src/data/patients.ts (Plan 01 — typed PATIENTS data)
    - frontend/src/components/ui/Modal.tsx (Phase 1 — modal component)
    - frontend/src/components/ui/Button.tsx, Input.tsx, Card.tsx (Phase 1 — UI primitives)
    - frontend/src/components/icons/index.tsx (Plan 01 — all icon components)
    - frontend/src/stores/navigationStore.ts (Plan 02 — for setActivePatientId, statusFilter)
  </read_first>
  <action>
    1. **Migrate `Avatar.tsx`** from `view_patients.jsx` Avatar function:
       - Props: `{ name: string; status?: PatientStatus; size?: number; src?: string }`
       - Renders circle with initials (first letter of first+last name) or image
       - Status dot: `.ontrack` → green (sage), `.warning` → amber, `.danger` → red (coral)
       - Size variants: small (32px default), large (48px for grid)
       - Status dot positioned at bottom-right of avatar circle

    2. **Migrate `Pagination.tsx`** from `view_patients.jsx` Pagination function:
       - Props: `{ currentPage: number; totalPages: number; onPageChange: (page: number) => void }`
       - Renders page numbers with prev/next arrows
       - Active page highlighted with `bg-lime text-ink`
       - Per prototype: `PAGE_SIZE = 6` for patient list pagination

    3. **Migrate `PatientTable.tsx`** from `view_patients.jsx` PatientTable function:
       - Table layout with columns: Avatar+Name, Status chip, Objective, Last consultation
       - Row click navigates to patient detail
       - Status chips use Tailwind: `bg-sage/15 text-sage` for on track, `bg-amber/15 text-amber` for warning, `bg-coral/15 text-coral` for danger
       - Hover state: `bg-surface-2`

    4. **Migrate `PatientGrid.tsx`** from `view_patients.jsx` PatientGrid function:
       - Card grid layout with patient cards showing Avatar, name, status, objective, macros summary
       - Per prototype: card with `bg-surface rounded-[var(--radius-lg)] border border-border`
       - Click card navigates to patient detail

    5. **Migrate `NewPatientModal.tsx`** from `view_patients.jsx` NewPatientModal:
       - Modal with form fields: Nome, Email, Telefone, Objetivo (dropdown), Data de nascimento
       - Uses `Modal` from `@/components/ui/Modal`
       - Uses `Input` from `@/components/ui/Input`
       - Uses `Button` from `@/components/ui/Button`
       - Form validation: name required, email format check
       - On submit: adds patient to PATIENTS array (mock — API comes in Phase 4)
       - pt-BR labels and placeholders

    6. **Migrate `EditPatientModal.tsx`** from `view_patients.jsx` EditPatientModal:
       - Similar modal to NewPatientModal but pre-filled with existing patient data
       - Props: `{ patient: Patient; open: boolean; onClose: () => void }`
       - Edits patient in local state (mock — no API yet)

    7. **Migrate `PatientsView.tsx`** from `view_patients.jsx`:
       - State: `mode` (table/grid), `search` query, `filterOpen`, `newPatientOpen`, `editingPatient`
       - Header with title "Pacientes", search input, table/grid toggle, "Novo paciente" button
       - Filter chips: Todos, No caminho (sage), Atenção (amber), Crítico (coral)
       - `useMemo` for filtered and paginated patient list:
         - Filter by `statusFilter` from `useNavigationStore`
         - Filter by `search` query (match name or objective)
         - Paginate with `PAGE_SIZE = 6`
       - Table/Grid mode toggle using `useState`
       - Import and render PatientTable or PatientGrid based on mode
       - Pagination component at bottom
       - NewPatientModal and EditPatientModal on top
       - Responsive: grid collapses to single column on mobile

    8. **Create `frontend/src/components/patients/index.ts`** barrel file.

    **All component names per CONVENTIONS.md:** PascalCase. All CSS via Tailwind utility classes. All Portuguese text from prototype preserved exactly.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && echo "PatientsView and subcomponents compile"</automated>
  </verify>
  <done>PatientsView renders patient list with table/grid toggle, status filters, search, pagination. NewPatientModal and EditPatientModal open/close with forms. Avatar shows status dot. All matching prototype layout and interactions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side data | Patient data is mock — no real API calls yet (Phase 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | S | NewPatientModal | accept | Mock data mutation — no real backend storage until Phase 4 |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- HomeView renders 4 KPI cards with PATIENTS and AGGREGATE data
- PatientsView table mode shows all 12 patients with columns
- PatientsView grid mode shows patient cards with avatars
- Status filter chips work (Todos, No caminho, Atenção, Crítico)
- Search filters patient list by name
- Pagination works with PAGE_SIZE=6
- Table/Grid toggle switches layout
- NewPatientModal opens with form fields
- EditPatientModal opens with pre-filled data
- Avatar shows status dot (sage/amber/coral)
- No window globals
</verification>

<success_criteria>
- HomeView with KPI cards and patient grid matching prototype
- PatientsView with table/grid toggle, search, status filters, pagination
- NewPatientModal and EditPatientModal with form validation
- Avatar component with status dot
- Pagination component
- All mock data renders correctly from typed constants
- All text in pt-BR
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-migration/02-04-SUMMARY.md`
</output>