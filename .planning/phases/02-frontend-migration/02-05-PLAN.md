---
phase: 02-frontend-migration
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-04"]
files_modified:
  - frontend/src/views/PatientView.tsx
  - frontend/src/components/patient/Timeline.tsx
  - frontend/src/components/patient/ExtractionEditor.tsx
  - frontend/src/components/patient/EditPatientModal.tsx
  - frontend/src/components/patient/NewBiometryModal.tsx
  - frontend/src/components/patient/index.ts
autonomous: true
requirements:
  - INFRA-02
must_haves:
  truths:
    - "Patient detail view renders 5 tabs (Hoje, Plano, Biometria, Insights, Histórico) matching prototype"
    - "Timeline component shows meal events with inline extraction editing"
    - "Biometry section shows weight and body fat charts over time"
    - "All SVG visualizations (Ring, MacroRings, LineChart) render in patient detail"
    - "Edit Patient modal pre-fills existing data"
  artifacts:
    - path: "frontend/src/views/PatientView.tsx"
      provides: "Patient detail with 5-tab navigation"
      min_lines: 300
    - path: "frontend/src/components/patient/Timeline.tsx"
      provides: "Meal timeline with extraction editing"
      exports: ["Timeline"]
    - path: "frontend/src/components/patient/ExtractionEditor.tsx"
      provides: "Inline extraction editor for meal entries"
      exports: ["ExtractionEditor"]
  key_links:
    - from: "frontend/src/views/PatientView.tsx"
      to: "frontend/src/data/ana.ts"
      via: "import ANA for detailed patient data"
      pattern: "import.*ANA"

---

<objective>
Migrate the PatientView — the most complex view in the prototype (944 lines) — with its 5 tabs, timeline, extraction editor, biometry charts, and edit modals. This is the clinical heart of the application.

Purpose: The patient detail view is where nutritionists spend most of their time. Getting it right means the migration preserves the core value proposition.
Output: Fully functional PatientView with all 5 tabs matching the prototype.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@view_patient.jsx — PatientView (944 lines) with 5 tabs, Timeline, ExtractionEditor, NewBiometryModal, MultiLineChart, EditPatientModal
@frontend/src/data/ana.ts — ANA detailed patient data (Plan 01)
@frontend/src/data/patients.ts — PATIENTS array (Plan 01)
@frontend/src/components/viz/ — Ring, MacroRings, Sparkline, LineChart, StackBar (Plan 01)
@frontend/src/components/icons/index.tsx — All icons (Plan 01)
@frontend/src/components/ui/ — Button, Input, Card, Modal (Phase 1)
@frontend/src/stores/navigationStore.ts — activePatientId, setView (Plan 02)

<interfaces>
From frontend/src/data/ana.ts:
```typescript
export const ANA: DetailedPatient;
// Contains: timeline, biometry, skinfolds, perimetry, macros
```

From frontend/src/types/patient.ts:
```typescript
export interface DetailedPatient extends Patient {
  timeline: TimelineEvent[];
  skinfolds: SkinfoldEntry[];
  perimetry: PerimetryEntry[];
  // ... full detail fields
}
```

From frontend/src/components/viz/:
```typescript
export function Ring({ percentage, ... }: RingProps)
export function MacroRings({ macros, ... }: MacroRingsProps)
export function LineChart({ data, ... }: LineChartProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Migrate PatientView with tabs, timeline, and biometry</name>
  <files>frontend/src/views/PatientView.tsx, frontend/src/components/patient/Timeline.tsx, frontend/src/components/patient/ExtractionEditor.tsx, frontend/src/components/patient/NewBiometryModal.tsx, frontend/src/components/patient/index.ts</files>
  <read_first>
    - view_patient.jsx (944 lines — the most complex view with 5 tabs, timeline, extraction editing, biometry)
    - data.jsx (ANA detailed patient data structure with timeline events, biometry)
    - frontend/src/data/ana.ts (Plan 01 — typed ANA data)
    - frontend/src/components/viz/Ring.tsx, MacroRings.tsx, LineChart.tsx (Plan 01 — viz components)
    - frontend/src/components/ui/Modal.tsx, Input.tsx, Button.tsx (Phase 1 — UI primitives)
    - frontend/src/stores/navigationStore.ts (Plan 02 — activePatientId)
  </read_first>
  <action>
    1. **Migrate `Timeline.tsx`** from `view_patient.jsx` Timeline function:
       - Props: `{ events: TimelineEvent[]; onEdit?: (event: TimelineEvent) => void }`
       - Renders vertical timeline with date markers, event cards, and meal extraction indicators
       - Event kinds: 'plan' (blue/lime), 'log' (green/sage), 'pending' (amber), 'upcoming' (gray)
       - Each event card shows: date, title, description, macro summary (if applicable)
       - AI-extracted entries marked with sparkle icon and `.chip.ai` (lime dim background)
       - Click on extraction event opens `ExtractionEditor`

    2. **Migrate `ExtractionEditor.tsx`** from `view_patient.jsx` ExtractionEditor:
       - Props: `{ event: TimelineEvent; onSave: (updated: TimelineEvent) => void; onClose: () => void }`
       - Inline editing of extracted meal data
       - Shows original extraction (what AI detected) and editable fields
       - Macro totals at bottom with Ring/MacroRings visualization
       - "Salvar" and "Cancelar" buttons

    3. **Migrate `NewBiometryModal.tsx`** from `view_patient.jsx` NewBiometryModal:
       - Modal with form fields: Data, Peso, % Gordura, Massa magra, Taxa metabólica basal
       - Date picker for consultation date
       - Number inputs for biometric values
       - Uses `Modal` from `@/components/ui/Modal`
       - Form validation: date required, at least one biometric value

    4. **Migrate `PatientView.tsx`** from `view_patient.jsx` (944 lines → TypeScript):
       - Find patient data: `useNavigationStore().activePatientId` → find in `PATIENTS` or use `ANA` for detailed view
       - Tab navigation state: `useState('today')` with tabs: 'Hoje', 'Plano', 'Biometria', 'Insights', 'Histórico'
       - Tab bar styling: `.seg` segmented control style from prototype
       - **Today tab (default):** Shows today's macro summary (MacroRings component), timeline of recent events, upcoming consultations
       - **Plano tab:** Placeholder meal plan view (PlansView integration in Plan 05)
       - **Biometria tab:** Weight and body fat charts over time using `LineChart`, skinfold and perimetry data tables, "Nova avaliação" button to open NewBiometryModal
       - **Insights tab:** AI-generated insights, adherence chart (StackBar), summary stats
       - **Histórico tab:** Full timeline of all events with filters
       - Header section: Patient name, status chip, objective, Edit button (opens EditPatientModal)
       - Breadcrumb: "← Pacientes" link back to patient list
       - Handle URL parameter: `/patient/:id` route parameter maps to `activePatientId`
       - All text in pt-BR matching prototype

    5. **Create `frontend/src/components/patient/index.ts`** barrel export.

    **Migration notes for this complex view:**
    - `view_patient.jsx` is 944 lines — the migrated version will be more modular with extracted sub-components
    - The `_MultiLineChart` inline component → use `LineChart` from `@/components/viz/`
    - The `_NewBiometryModal` → separate `NewBiometryModal.tsx` component
    - The `_Timeline` → separate `Timeline.tsx` component
    - The `_ExtractionEditor` → separate `ExtractionEditor.tsx` component
    - All `React.useState` → destructured imports
    - All `React.useMemo` → destructured imports
    - Per CONVENTIONS.md: tab state pattern `const [tab, setTab] = useState('today')`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && echo "PatientView compiles successfully"</automated>
  </verify>
  <done>PatientView renders 5 tabs with full patient detail data. Timeline shows meal events. Biometry shows charts. Extraction editor allows inline editing. All matching prototype interactions and visual layout.</done>
</task>

<task type="auto">
  <name>task 2: Integration verification and final build</name>
  <files>frontend/src/views/PatientView.tsx, frontend/package.json</files>
  <read_first>
    - frontend/src/App.tsx (verify all routes and imports)
    - frontend/src/views/HomeView.tsx, PatientsView.tsx, PatientView.tsx (verify imports)
    - frontend/src/components/patients/ (verify all sub-components)
    - frontend/src/components/patient/ (verify all sub-components)
    - frontend/src/components/viz/ (verify all viz components)
    - frontend/src/components/icons/ (verify all icons)
    - frontend/package.json (verify all dependencies)
  </read_first>
  <action>
    1. **Verify all imports are wired correctly:**
       - `App.tsx` imports all 9 view components: HomeView, PatientsView, PatientView, FoodsView, InsightsView, LandingView, LoginView, SignupView, OnboardingView
       - All view components import from correct paths (`@/data/`, `@/components/`, `@/stores/`, `@/types/`)
       - No circular dependencies exist (check with `npx madge --circular frontend/src/`)
       - No `window.` global references remain

    2. **Verify Vite production build:**
       - Run `cd frontend && npm run build` — must succeed with zero Babel standalone dependency
       - Build output should produce static assets in `frontend/dist/`
       - Verify build has no TypeScript errors

    3. **Verify theme parity:**
       - Light theme renders identically to prototype (all colors, fonts, spacing)
       - Dark theme renders identically to prototype (all colors, fonts, spacing)
       - Theme toggle persists in localStorage

    4. **Verify responsive behavior:**
       - Sidebar collapses at 1200px viewport width
       - Landing page layout adjusts at 900px and 600px breakpoints
       - Patient grid/table responsive at different widths

    5. **Fix any issues found during integration verification.**
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build && echo "Production build successful"</automated>
  </verify>
  <done>All views compile and import correctly. Vite production build succeeds. Theme parity confirmed. No window globals. No Babel standalone dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side data | Patient detail data is from ANA mock — no real API calls yet |
| Large component | PatientView is 944 lines in prototype — modularized into sub-components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | I | Biometry form | accept | Client-side number validation only — server validation in Phase 4+ |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- `cd frontend && npm run build` produces production bundle
- PatientView renders with 5 tabs
- Timeline component shows meal events with extraction marker
- Biometry tab shows charts with LineChart
- No window global references in any migrated code
- No Babel standalone in production build
- All routes navigate correctly (React Router)
- Theme toggle works on all views
</verification>

<success_criteria>
- PatientView with 5 tabs (Hoje, Plano, Biometria, Insights, Histórico) matching prototype
- Timeline component with meal event cards and extraction indicators
- Extraction editor for inline meal data editing
- Biometry section with LineChart visualization
- Edit patient modal with pre-filled data
- Production build succeeds with zero Babel standalone
- All imported modules resolve correctly
- No window globals
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-migration/02-05-SUMMARY.md`
</output>