---
phase: 02-frontend-migration
plan: 06
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - frontend/src/views/PlansView.tsx
  - frontend/src/views/FoodsView.tsx
  - frontend/src/views/InsightsView.tsx
autonomous: true
requirements:
  - INFRA-02
must_haves:
  truths:
    - "Meal plan editor renders meals with options, food rows, and macro totals matching prototype"
    - "Food catalog renders base items (per 100g) and preset items (portioned) with search and pagination"
    - "Insights view renders aggregate statistics with charts matching prototype"
    - "All SVG visualizations (StackBar, Ring, MacroRings) render in their respective views"
  artifacts:
    - path: "frontend/src/views/PlansView.tsx"
      provides: "Meal plan editor with inline food editing"
      min_lines: 300
    - path: "frontend/src/views/FoodsView.tsx"
      provides: "Food catalog with search, categories, pagination"
      min_lines: 150
    - path: "frontend/src/views/InsightsView.tsx"
      provides: "Aggregate intelligence dashboard"
      min_lines: 80
  key_links:
    - from: "frontend/src/views/PlansView.tsx"
      to: "frontend/src/data/ana.ts"
      via: "import ANA for meal plan data"
      pattern: "import.*ANA"
    - from: "frontend/src/views/FoodsView.tsx"
      to: "frontend/src/data/foods.ts"
      via: "import FOODS_CATALOG for food items"
      pattern: "import.*FOODS_CATALOG"

---

<objective>
Migrate the remaining clinical views: PlansView (meal plan editor), FoodsView (food catalog), and InsightsView (aggregate intelligence dashboard). These complete the authenticated-only views.

Purpose: These views represent the core clinical workflow — creating meal plans, browsing the food catalog, and reviewing patient insights. Completing them finishes all UI migration.
Output: Three fully migrated views that complete the set of 9 prototype views.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@view_plans.jsx — PlansView (884 lines) — meal plan editor with meals, options, food rows, inline editing, macro totals, extras
@view_foods.jsx — FoodsView (340 lines) — food catalog with search, food cards (base/preset), categories, pagination, AddFoodModal
@view_insights.jsx — InsightsView (180 lines) — aggregate intelligence dashboard with CarteiraChart, adherence metrics
@frontend/src/data/foods.ts — FOODS_CATALOG and FOOD_CATEGORIES (Plan 01)
@frontend/src/data/ana.ts — ANA detailed patient data (Plan 01)
@frontend/src/data/aggregate.ts — AGGREGATE stats (Plan 01)
@frontend/src/components/viz/ — StackBar, Ring, MacroRings, Sparkline (Plan 01)
@frontend/src/components/icons/index.tsx — All icons (Plan 01)
@frontend/src/components/ui/ — Button, Input, Card, Modal (Phase 1)

<interfaces>
From frontend/src/types/food.ts:
```typescript
export interface Food { id: string; name: string; category: string; type: FoodType; ... }
export type FoodType = 'base' | 'preset';
export const FOODS_CATALOG: Food[];
export const FOOD_CATEGORIES: string[];
```

From frontend/src/types/plan.ts:
```typescript
export interface MealSlot { id: string; name: string; time: string; options: MealOption[]; }
export interface MealOption { id: string; foods: MealFood[]; total: MacroTarget; }
export interface MealFood { foodId: string; name: string; quantity: number; unit: string; macros: MacroTarget; }
export interface PlanExtra { id: string; name: string; quantity: string; macros: MacroTarget; }
```

From frontend/src/data/ana.ts:
```typescript
export const ANA: DetailedPatient;
// Contains initial meals and options from prototype
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Migrate PlansView with meal plan editor</name>
  <files>frontend/src/views/PlansView.tsx</files>
  <read_first>
    - view_plans.jsx (884 lines — complex meal plan editor with inline food editing, macros, extras)
    - data.jsx (INITIAL_MEALS, INITIAL_OPTIONS, INITIAL_EXTRAS mock data)
    - frontend/src/types/plan.ts (Plan 01 — MealSlot, MealOption, MealFood, PlanExtra types)
    - frontend/src/data/ana.ts (Plan 01 — ANA patient with meal data)
    - frontend/src/components/viz/MacroRings.tsx (Plan 01 — macro visualization)
    - frontend/src/data/foods.ts (Plan 01 — FOODS_CATALOG for food lookup)
    - frontend/src/components/icons/index.tsx (Plan 01 — all icons)
  </read_first>
  <action>
    Migrate `PlansView.tsx` from `view_plans.jsx` (884 lines). This is the most complex view after PatientView. Break it into manageable sections:

    1. **State management:**
       - `meals: MealSlot[]` — initialized from `ANA.meals` or `INITIAL_MEALS` mock data
       - `extras: PlanExtra[]` — out-of-plan food authorizations
       - `mode: 'table' | 'edit'` — view mode toggle
       - `editingFood: { mealIndex: number; optionIndex: number; foodIndex: number } | null`
       - Inline editing state for food rows within meals

    2. **Meal structure rendering:**
       - Each meal (café da manhã, lanche da manhã, almoço, etc.) rendered as a card
       - Each meal has options (turmas) — option A vs option B
       - Each option has individual food rows: food name, quantity, unit, macros per item
       - Food rows are inline-editable: click to edit quantity, unit, or food name
       - "Add food" button per meal option → inserts blank food row
       - "Add meal" button → inserts new meal slot at bottom
       - Option toggle tabs (Option A / Option B)

    3. **Macro totals:**
       - Per-meal subtotal macros (kcal, protein, carbs, fat) displayed at bottom of each meal card
       - Daily total macros displayed at top/bottom with MacroRings visualization
       - Computed via `useMemo` from current meals state
       - Status colors: protein → sage, carbs → amber, fat → sky, kcal → ink-contrast

    4. **Extras section:**
       - "Alimentos extras" section below meals
       - Each extra has name, quantity, macros
       - "Adicionar extra" button to add new extra
       - Extras are foods authorized outside the meal plan

    5. **Interactions matching prototype:**
       - Inline edit: click on food quantity → editable input with save on Enter/blur
       - Add food: opens food search within meal → select from FOODS_CATALOG
       - Remove food: trash icon on hover
       - Switch options: click tab to switch between meal options A/B
       - Macro rings update reactively as foods are added/removed/edited
       - All text in pt-BR: "Café da manhã", "Almoço", "Lanche da tarde", "Jantar", "Ceia", "Opção A", "Opção B", "Adicionar alimento", "Alimentos extras"

    6. **Styling:**
       - Meal cards use Tailwind: `bg-surface rounded-[var(--radius-lg)] border border-border`
       - Food rows use mono font for macros: `.mono .tnum`
       - Macro totals use font-serif for section headings, font-mono for values
       - Active option tab: `border-b-2 border-lime text-lime`
       - Inactive option tab: `text-fg-subtle hover:text-fg`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && echo "PlansView compiles successfully"</automated>
  </verify>
  <done>PlansView renders meal plan editor with inline food editing, option toggles, macro totals, extras section. MacroRings updates reactively. All matching prototype interactions and layout. pt-BR text throughout.</done>
</task>

<task type="auto">
  <name>task 2: Migrate FoodsView and InsightsView</name>
  <files>frontend/src/views/FoodsView.tsx, frontend/src/views/InsightsView.tsx</files>
  <read_first>
    - view_foods.jsx (340 lines — food catalog with search, categories, base/preset cards, AddFoodModal, pagination)
    - view_insights.jsx (180 lines — aggregate intelligence dashboard)
    - frontend/src/data/foods.ts (Plan 01 — FOODS_CATALOG, FOOD_CATEGORIES)
    - frontend/src/data/aggregate.ts (Plan 01 — AGGREGATE stats)
    - frontend/src/components/viz/StackBar.tsx, Ring.tsx (Plan 01 — viz components for insights)
    - frontend/src/components/ui/Modal.tsx, Button.tsx, Input.tsx (Phase 1)
  </read_first>
  <action>
    1. **Migrate `FoodsView.tsx`** from `view_foods.jsx` (340 lines):
       - Food catalog grid showing all FOODS_CATALOG items
       - Category filter chips: "Todos", then each category from FOOD_CATEGORIES
       - Search input filtering foods by name (case-insensitive)
       - Two card types:
         - **Base items** (`type: 'base'`): Show per-100g nutrition data, food name, calories per 100g
         - **Preset items** (`type: 'preset'`): Show portioned data, food name, calories with serving size
       - Card styling: `bg-surface rounded-[var(--radius-lg)] border border-border p-4`
       - Macro summary on each card: kcal (ink-contrast), protein (sage), carbs (amber), fat (sky)
       - AddFoodModal: opens when clicking "Adicionar alimento" button
         - Form fields: Nome, Categoria, Tipo (base/preset), Porção (for preset), Calories, Protein, Carbs, Fat per 100g
         - Uses `Modal`, `Input`, `Button` from `@/components/ui/`
       - Pagination: `PAGE_SIZE = 8` items per page
       - State: `q` (search query), `category` (selected filter), `page` (current page), `addFoodOpen` (modal state)

    2. **Migrate `InsightsView.tsx`** from `view_insights.jsx` (180 lines):
       - Page title "Inteligência" with icon
       - Summary cards at top:
         - "Pacientes ativos" — count from AGGREGATE
         - "Adesão média" — percentage with Ring visualization
         - "Refeições registradas" — count
         - "Tendência" — up/down with Sparkline
       - CarteiraChart: StackBar visualization showing macro distribution across patients
       - Patient adherence list: ranked list of patients with adherence percentages
       - All stats from `AGGREGATE` mock data
       - All text in pt-BR: "Inteligência", "Pacientes ativos", "Adesão média", "Refeições registradas", "Carteira"

    **Styling rules for both:**
    - All colors via Tailwind theme tokens (bg-surface, text-fg, border-border, etc.)
    - Status colors: sage (ontrack), amber (warning), coral (danger)
    - Typography: `.eyebrow` for labels, `.mono .tnum` for data values, `.serif` for section titles
    - Cards: `bg-surface rounded-[var(--radius-lg)] border border-border`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && echo "FoodsView and InsightsView compile"</automated>
  </verify>
  <done>FoodsView renders food catalog with search, category filter, base/preset cards, pagination, and add modal. InsightsView renders aggregate stats with visualizations. All matching prototype.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side data | Food catalog and meal plans use mock data — no real API |
| Inline editing | Meal plan editing modifies local state only — no persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | S | AddFoodModal | accept | Mock form — no backend validation until Phase 5 |
| T-02-09 | I | PlansView inline edit | accept | Local state mutation — no persistence, no XSS risk with React |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- PlansView renders meal cards with options, food rows, macro totals
- Food rows are inline-editable
- MacroRings updates when foods are added/removed
- Extras section shows out-of-plan foods
- FoodsView renders food catalog with base/preset distinction
- Food search filters by name
- Category filter chips work
- Pagination works with PAGE_SIZE=8
- AddFoodModal opens with form fields
- InsightsView renders stats cards and StackBar
- All text is pt-BR
- No window globals in any view
</verification>

<success_criteria>
- PlansView fully functional with meal editing, option toggles, macro totals, extras
- FoodsView with search, categories, base/preset cards, pagination, add modal
- InsightsView with aggregate stats and visualizations
- All 9 prototype views fully migrated to TypeScript
- All mock data renders correctly
- All interactions work (modals, inline editing, filters, pagination)
- All pt-BR text preserved from prototype
- TypeScript compilation passes
- No window globals
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-migration/02-06-SUMMARY.md`
</output>