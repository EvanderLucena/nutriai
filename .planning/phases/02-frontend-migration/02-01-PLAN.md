---
phase: 02-frontend-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/icons/index.tsx
  - frontend/src/data/patients.ts
  - frontend/src/data/ana.ts
  - frontend/src/data/aggregate.ts
  - frontend/src/data/foods.ts
  - frontend/src/data/index.ts
  - frontend/src/components/viz/Ring.tsx
  - frontend/src/components/viz/MacroRings.tsx
  - frontend/src/components/viz/Sparkline.tsx
  - frontend/src/components/viz/WeekBars.tsx
  - frontend/src/components/viz/LineChart.tsx
  - frontend/src/components/viz/StackBar.tsx
  - frontend/src/components/viz/index.ts
  - frontend/src/types/patient.ts
  - frontend/src/types/food.ts
  - frontend/src/types/plan.ts
  - frontend/src/types/biometry.ts
  - frontend/src/types/index.ts
autonomous: true
requirements:
  - INFRA-02
must_haves:
  truths:
    - "All 29 SVG icons from icons.jsx compile as named TypeScript exports with proper React SVG props"
    - "Mock data structures (PATIENTS, ANA, AGGREGATE, FOODS_CATALOG) are typed constants that downstream views can import"
    - "All 6 visualization components (Ring, MacroRings, Sparkline, WeekBars, LineChart, StackBar) render SVGs matching the prototype"
    - "TypeScript type definitions cover Patient, Food, MealPlan, Biometry, and related domain shapes"
  artifacts:
    - path: "frontend/src/components/icons/index.tsx"
      provides: "29+ icon components as named exports"
      min_lines: 200
    - path: "frontend/src/data/index.ts"
      provides: "Barrel export for all mock data"
    - path: "frontend/src/components/viz/index.ts"
      provides: "Barrel export for all viz components"
      exports: ["Ring", "MacroRings", "Sparkline", "WeekBars", "LineChart", "StackBar"]
    - path: "frontend/src/types/patient.ts"
      provides: "Patient, PatientStatus, MacroTarget types"
      contains: "export type PatientStatus"
    - path: "frontend/src/types/food.ts"
      provides: "Food, FoodCategory, FoodType types"
      contains: "export interface Food"
  key_links:
    - from: "frontend/src/components/viz/MacroRings.tsx"
      to: "frontend/src/types/patient.ts"
      via: "import type { MacroTarget } from"
      pattern: "import.*MacroTarget.*from"

---

<objective>
Migrate the foundational shared layer from the prototype to TypeScript modules — icons, data types/constants, and SVG visualizations. These are the building blocks that all downstream views depend on.

Purpose: Establish typed, ES-module versions of every shared component and constant so Wave 2+ views can import them without window globals.
Output: 29 icon components, typed mock data for 12 patients + detailed patient + aggregate stats + food catalog, 6 visualization components, and domain type definitions.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-infrastructure/01-CONTEXT.md
</execution_context>

<context>
@icons.jsx — 29 SVG icon components registered on window
@data.jsx — PATIENTS (12 items), ANA (detailed patient), AGGREGATE (portfolio stats), mock data constants
@viz.jsx — Ring, MacroRings, Sparkline, WeekBars, LineChart, StackBar/MultiLineChart visualization components
@frontend/src/styles/globals.css — Tailwind theme tokens already defined (Phase 1)
@frontend/src/types/index.ts — Existing PatientStatus and ViewType types (Phase 1)

<interfaces>
<!-- Phase 1 scaffold types already defined -->

From frontend/src/types/index.ts:
```typescript
export type PatientStatus = 'ontrack' | 'warning' | 'danger';
export type ViewType = 'home' | 'patients' | 'patient' | 'foods' | 'insights';
export type AuthViewType = 'landing' | 'login' | 'signup' | null;
```

From frontend/src/styles/globals.css (Tailwind v4 @theme):
```css
/* Colors available as Tailwind utilities: bg-lime, text-coral, border-sage, etc. */
/* Semantic colors: bg-surface, text-fg, border-border, etc. */
/* Font families: font-ui, font-mono, font-serif */
```

From frontend/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Migrate icons and create domain types</name>
  <files>frontend/src/components/icons/index.tsx, frontend/src/types/patient.ts, frontend/src/types/food.ts, frontend/src/types/plan.ts, frontend/src/types/biometry.ts, frontend/src/types/index.ts</files>
  <behavior>
    - Test: Each icon component renders without error (smoke test)
    - Test: PatientStatus type narrows to 'ontrack' | 'warning' | 'danger'
    - Test: Food interface has required fields (id, name, category, type, macros)
    - Test: MacroTarget has kcal, prot, carb, fat each with target and actual
  </behavior>
  <read_first>
    - icons.jsx (source prototype icons to migrate)
    - data.jsx (mock data structure — defines domain shapes)
    - frontend/src/types/index.ts (existing Phase 1 types to extend)
    - .planning/codebase/STACK.md (prototype patterns)
    - .planning/codebase/CONVENTIONS.md (naming conventions)
  </read_first>
  <action>
    1. Create `frontend/src/types/patient.ts` with TypeScript interfaces matching prototype data structures:
       - `PatientStatus` type: `'ontrack' | 'warning' | 'danger'` (move from index.ts)
       - `Patient` interface: id, name, email, phone, objective, status, avatar, age, weight, height, macros (MacroTarget[]), createdAt
       - `MacroTarget` interface: `{ kcal: { target: number; actual: number }; prot: { target: number; actual: number }; carb: { target: number; actual: number }; fat: { target: number; actual: number } }`
       - `BiometricEntry` interface: date, weight, bodyFat, muscleMass, basalMetabolicRate
       - `TimelineEvent` interface: id, date, kind ('plan' | 'log' | 'pending' | 'upcoming'), title, description, macros

    2. Create `frontend/src/types/food.ts`:
       - `FoodType`: `'base' | 'preset'`
       - `Food` interface: id, name, category, type, portion, unit, calories, protein, carbs, fat, basePer100g
       - `FoodCategory` type for food group labels

    3. Create `frontend/src/types/plan.ts`:
       - `MealSlot` interface: id, name, time, options: MealOption[]
       - `MealOption` interface: id, foods: MealFood[], total: MacroTarget
       - `MealFood` interface: foodId, name, quantity, unit, macros
       - `PlanExtra` interface: id, name, quantity, macros

    4. Create `frontend/src/types/biometry.ts`:
       - `SkinfofoldEntry` interface: date, values (map of skinfold sites)
       - `PerimetryEntry` interface: date, values (map of body sites)

    5. Update `frontend/src/types/index.ts` to re-export all types and remove PatientStatus/ViewType/AuthViewType (moved to specific type files).

    6. Create `frontend/src/components/icons/index.tsx` — migrate all 29 icons from `icons.jsx`:
       - Each icon becomes a named function component: `IconHome`, `IconUsers`, `IconPlan`, `IconInsight`, `IconBell`, `IconSettings`, `IconSearch`, `IconPlus`, `IconChevronR`, `IconChevronD`, `IconSparkle`, `IconWhatsapp`, `IconClock`, `IconTrend`, `IconMeal`, `IconScale`, `IconEdit`, `IconFilter`, `IconDots`, `IconAlert`, `IconCheck`, `IconX`, `IconArrowR`, `IconCalendar`, `IconDrop`, `IconTrash`, `IconArchive`, `IconDownload`, `IconFood` (add new missing one if needed)
       - All accept `IconProps`: `{ size?: number; className?: string; color?: string }` with sensible defaults (size=24, color='currentColor')
       - SVG viewBox, stroke/fill attributes preserved exactly from prototype
       - Per AGENTS.md naming conventions: PascalCase with `Icon` prefix
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20 && echo "TypeScript compilation check passed"</automated>
  </verify>
  <done>All icon components compile as TypeScript with proper exports. Domain types defined for Patient, Food, MealPlan, Biometry shapes. No window globals used — all imports are ES module style.</done>
</task>

<task type="auto">
  <name>task 2: Migrate mock data to typed constants</name>
  <files>frontend/src/data/patients.ts, frontend/src/data/ana.ts, frontend/src/data/aggregate.ts, frontend/src/data/foods.ts, frontend/src/data/index.ts</files>
  <read_first>
    - data.jsx (mock patient data, ANA detail object, AGGREGATE stats, food catalog items)
    - frontend/src/types/patient.ts (created in task 1 — types used here)
    - frontend/src/types/food.ts (created in task 1 — Food interface)
  </read_first>
  <action>
    1. Create `frontend/src/data/patients.ts`:
       - Export `PATIENTS` constant typed as `Patient[]` — migrate all 12 patient objects from `data.jsx` `PATIENTS` array
       - Each patient's `status` field uses the `PatientStatus` literal type
       - All Portuguese text preserved exactly (pt-BR)
       - Use `as const satisfies` pattern where appropriate for type safety

    2. Create `frontend/src/data/ana.ts`:
       - Export `ANA` constant typed with the detailed patient interface — migrate the full ANA object with timeline, biometry, skinfolds, perimetry data from `data.jsx`
       - Include `skinfolds`, `perimetry`, `timeline`, `macros` sub-objects matching the prototype structure exactly

    3. Create `frontend/src/data/aggregate.ts`:
       - Export `AGGREGATE` constant — migrate portfolio-level stats (activePatients, adherence, trends, alerts) from `data.jsx`

    4. Create `frontend/src/data/foods.ts`:
       - Export `FOODS_CATALOG` typed as `Food[]` — migrate all 17 food items (12 bases + 5 presets) from `view_foods.jsx` `FOODS_CATALOG` array
       - Export `FOOD_CATEGORIES` constant — food category labels from `view_foods.jsx`

    5. Create `frontend/src/data/index.ts` barrel exports:
       - Re-export everything from all data files
       - `export { PATIENTS } from './patients'`, etc.

    All data must match the prototype values exactly — this is a migration, not a redesign. Numbers, names, Portuguese text, status strings all preserved verbatim.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && echo "All data files type-check successfully"</automated>
  </verify>
  <done>PATIENTS, ANA, AGGREGATE, FOODS_CATALOG, and FOOD_CATEGORIES are typed constants importable from src/data/. Values match prototype exactly. No window globals.</done>
</task>

<task type="auto">
  <name>task 3: Migrate visualization components</name>
  <files>frontend/src/components/viz/Ring.tsx, frontend/src/components/viz/MacroRings.tsx, frontend/src/components/viz/Sparkline.tsx, frontend/src/components/viz/WeekBars.tsx, frontend/src/components/viz/LineChart.tsx, frontend/src/components/viz/StackBar.tsx, frontend/src/components/viz/index.ts</files>
  <read_first>
    - viz.jsx (all 6 visualization components — Ring, MacroRings, Sparkline, WeekBars, LineChart, StackBar)
    - frontend/src/styles/globals.css (Tailwind theme color tokens: --lime, --coral, --sage, --amber, --sky)
    - frontend/src/types/patient.ts (MacroTarget type used by MacroRings)
    - data.jsx (mock data used by viz components for dimensions and values)
  </read_first>
  <action>
    Migrate each visualization component from `viz.jsx` to individual TypeScript files in `frontend/src/components/viz/`:

    1. `Ring.tsx` — Circular progress ring SVG component. Props: `{ percentage: number; size?: number; strokeWidth?: number; color?: string; bgColor?: string; className?: string }`. Render SVG circle with stroke-dasharray animation matching prototype. Use Tailwind color tokens via CSS custom properties (e.g., `stroke="var(--lime)"`).

    2. `MacroRings.tsx` — Three concentric rings showing protein/carbs/fat vs targets. Props: `{ macros: MacroTarget; size?: number; className?: string }`. Render protein (sage), carbs (amber), fat (sky) rings with labels. Match prototype layout: ring labels inside, percentage text below.

    3. `Sparkline.tsx` — Mini SVG line chart. Props: `{ data: number[]; width?: number; height?: number; color?: string; className?: string }`. Render polyline from data points. Match prototype's minimal sparkline style.

    4. `WeekBars.tsx` — 7-day bar chart. Props: `{ data: number[]; labels?: string[]; color?: string; className?: string }`. Render 7 SVG rect bars with hover area. Match prototype bar spacing and sizing.

    5. `LineChart.tsx` — Multi-point line chart with grid lines. Props: `{ data: { label: string; value: number }[]; color?: string; showGrid?: boolean; className?: string }`. Render SVG path with optional grid lines. Match prototype axis labels and grid style.

    6. `StackBar.tsx` — Stacked horizontal bar showing macro composition. Props: `{ segments: { value: number; color: string; label: string }[]; total: number; className?: string }`. Render SVG rects side by side. Match prototype's bar proportions and colors.

    7. `index.ts` — Barrel export: `export { Ring } from './Ring'`, etc.

    **Critical migration rules:**
    - Convert all `React.useState` to `useState` imports (destructured, not global)
    - Convert all `React.useMemo` to `useMemo` imports
    - Replace all inline `style={{}}` color values that reference CSS custom properties with direct `var(--token)` references (e.g., `fill="var(--lime)"` in SVG)
    - Replace all hardcoded hex colors from prototype with CSS custom property references where they correspond to theme tokens
    - Components must accept explicit props (not rely on window globals or prototype's prop-drilling patterns from App)
    - All SVG viewBox, path data, and dimensions preserved exactly from prototype
    - Per CONVENTIONS.md: `.mono` class for data values, `.tnum` for number alignment, `.serif` for headings
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && echo "All viz components compile" && grep -r "window\." src/components/viz/ || echo "No window globals in viz components"</automated>
  </verify>
  <done>All 6 visualization components migrated to TypeScript with proper ES module imports. No window globals. SVG rendering matches prototype. Tailwind color tokens used throughout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None for this plan | This plan migrates TypeScript types, constants, and UI visualization components — all client-side, no API calls, no user input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I | data files | accept | Mock data constants are client-side only — no sensitive data (all fictional patient data in Portuguese) |
</threat_model>

<verification>
- TypeScript compilation: `cd frontend && npx tsc --noEmit` exits 0
- No window globals: `grep -r "window\." src/` finds no references to prototype global patterns
- All icon components render: import each icon and verify it's a valid React component
- Viz components accept typed props: Ring, MacroRings, etc. compile with correct prop types
- Data constants match prototype: PATIENTS has 12 items, FOODS_CATALOG has 17 items, ANA has timeline/biometry
</verification>

<success_criteria>
- All 29 icon components exported as named TypeScript functions
- Domain types defined: Patient, Food, MealPlan, Biometry, and all sub-types
- Mock data constants (PATIENTS, ANA, AGGREGATE, FOODS_CATALOG) are typed and importable
- All 6 viz components compile with proper SVG rendering logic
- Zero window global references — all ES module imports
- `cd frontend && npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-migration/02-01-SUMMARY.md`
</output>