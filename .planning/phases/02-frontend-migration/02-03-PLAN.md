---
phase: 02-frontend-migration
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - frontend/src/views/LandingView.tsx
  - frontend/src/views/LoginView.tsx
  - frontend/src/views/SignupView.tsx
  - frontend/src/views/OnboardingView.tsx
autonomous: true
requirements:
  - INFRA-02
must_haves:
  truths:
    - "Landing page renders marketing content with pricing, features grid, and CTA buttons matching prototype"
    - "Login form accepts email and password with validation feedback matching prototype"
    - "Signup form has 2-step flow (credentials then profile) matching prototype steps"
    - "Onboarding wizard has 4 steps (carteira, plano, convite, pronto) matching prototype flow"
    - "All public views use proper Portuguese (pt-BR) text"
    - "Light and dark themes render correctly on all auth/landing views"
  artifacts:
    - path: "frontend/src/views/LandingView.tsx"
      provides: "Marketing landing page with hero, features, pricing, CTA"
      min_lines: 200
    - path: "frontend/src/views/LoginView.tsx"
      provides: "Email/password login form with validation"
      min_lines: 50
    - path: "frontend/src/views/SignupView.tsx"
      provides: "2-step signup wizard"
      min_lines: 80
    - path: "frontend/src/views/OnboardingView.tsx"
      provides: "4-step onboarding flow (carteira, plano, convite, pronto)"
      min_lines: 90
  key_links:
    - from: "frontend/src/views/LoginView.tsx"
      to: "frontend/src/stores/authStore.ts"
      via: "useAuthStore for login/logout"
      pattern: "useAuthStore"
    - from: "frontend/src/views/SignupView.tsx"
      to: "frontend/src/stores/authStore.ts"
      via: "useAuthStore for setAuthView"
      pattern: "useAuthStore"

---

<objective>
Migrate all public/unauthenticated views from the prototype — Landing, Login, Signup, and Onboarding. These views don't require authenticated state or API calls; they use the auth store for navigation.

Purpose: Complete the public-facing experience so users see the marketing page, can navigate to login/signup, and go through onboarding — all matching the prototype visually and interactively.
Output: 4 fully migrated views that work with React Router and Zustand auth store.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@view_landing.jsx — LandingView (499 lines) with hero, features, pricing cards, testimonials, footer
@view_login.jsx — LoginView (93 lines) with email/password form
@view_signup.jsx — SignupView (142 lines) with 2-step signup (credentials then profile)
@view_onboarding.jsx — OnboardingView (163 lines) with 4 steps (carteira, plano, convite, pronto)
@frontend/src/stores/authStore.ts — Auth state store (Plan 02)
@frontend/src/components/icons/index.tsx — Icon components (Plan 01)
@frontend/src/components/ui/Button.tsx — Button component (Phase 1)
@frontend/src/components/ui/Input.tsx — Input component (Phase 1)
@frontend/src/components/ui/Card.tsx — Card component (Phase 1)
@frontend/src/styles/globals.css — Tailwind theme + component styles (Plan 02)

<interfaces>
From frontend/src/stores/authStore.ts:
```typescript
export const useAuthStore = create<AuthState>(...);
// AuthState: { isAuthenticated, authView, login, logout, setAuthView }
```

From frontend/src/types/index.ts:
```typescript
export type AuthViewType = 'landing' | 'login' | 'signup' | null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Migrate LandingView and LoginView</name>
  <files>frontend/src/views/LandingView.tsx, frontend/src/views/LoginView.tsx</files>
  <read_first>
    - view_landing.jsx (full 499-line landing page — hero, features, pricing, CTAs, footer)
    - view_login.jsx (login form with email/password fields)
    - frontend/src/components/icons/index.tsx (icon components for landing page)
    - frontend/src/components/ui/Button.tsx (Button component)
    - frontend/src/components/ui/Input.tsx (Input component)
    - frontend/src/styles/globals.css (landing-* classes, auth-* classes)
  </read_first>
  <action>
    1. **Migrate `LandingView.tsx`** from `view_landing.jsx` (499 lines):
       - Translate all inline CSS styles to Tailwind utility classes
       - Map prototype's landing CSS classes (`.landing-hero`, `.landing-*`) to Tailwind equivalents or custom CSS in `globals.css`
       - Hero section: headline "NutriAI" with serif font, subheadline, CTA buttons (Começar agora, Ver planos)
       - Features grid: 6 feature cards with SVG icons, title, description — all in pt-BR
       - Pricing section: 3 plan cards (Iniciante R$99,99, Profissional R$149,99, Ilimitado R$199,99) with feature lists and CTA buttons
       - Footer with copyright and links
       - Responsive layout: 3-column grid on desktop, single column on mobile (breakpoints at 900px and 600px per prototype)
       - Logo and navigation that link to `/login` using React Router `<Link>` component
       - "Já tem conta? Entrar" link to `/login`
       - Landing page should NOT show the authenticated app shell — it's a standalone layout

    2. **Migrate `LoginView.tsx`** from `view_login.jsx` (93 lines):
       - Auth panel layout matching prototype: centered card with email/password fields
       - Use `Input` component from `@/components/ui/Input`
       - Use `Button` component for submit button
       - Password field with type="password"
       - Form validation: require both fields, email format check
       - On submit: call `useAuthStore().login()` — navigates to `/home` via React Router
       - Link to signup: "Não tem conta? Cadastre-se" → `/signup`
       - Auth background: subtle pattern or gradient matching prototype's `.auth-page` styles
       - pt-BR text throughout

    **Migration rules:**
    - All component inline styles → Tailwind utility classes or `globals.css` custom classes
    - All `React.useState` → destructured `useState` import
    - All `Object.assign(window, {...})` → named ES module imports
    - Per CONVENTIONS.md: form handler pattern `const [form, setForm] = useState({...}); const set = (k, v) => setForm(f => ({...f, [k]: v}));`
    - Use `<Link to="/path">` from React Router for navigation, not `setView('')`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && echo "Landing and Login views compile"</automated>
  </verify>
  <done>LandingView renders marketing page with hero, features, pricing, and footer matching prototype. LoginView renders auth form with validation. Both use React Router navigation and Zustand auth store.</done>
</task>

<task type="auto">
  <name>task 2: Migrate SignupView and OnboardingView</name>
  <files>frontend/src/views/SignupView.tsx, frontend/src/views/OnboardingView.tsx</files>
  <read_first>
    - view_signup.jsx (2-step signup: step 1 = email + password, step 2 = name + CRN + specialty)
    - view_onboarding.jsx (4-step onboarding: carteira, plano, convite, pronto)
    - frontend/src/stores/authStore.ts (for navigation after signup)
    - frontend/src/components/ui/Button.tsx, Input.tsx, Card.tsx (UI primitives)
    - frontend/src/styles/globals.css (onboard-* and auth-* classes)
  </read_first>
  <action>
    1. **Migrate `SignupView.tsx`** from `view_signup.jsx` (142 lines):
       - 2-step signup form:
         - Step 1: Email, Password, Confirm Password fields
         - Step 2: Full Name, CRN (nutritionist registration number), Specialty dropdown
       - Step indicator showing current step (1/2, 2/2)
       - "Voltar" button on step 2 to go back
       - Form validation per step: step 1 requires valid email + matching passwords (min 8 chars), step 2 requires name and CRN
       - On complete: call `useAuthStore().login()` → `/onboarding`
       - Link to login: "Já tem conta? Entrar" → `/login`
       - Same auth panel styling as LoginView
       - pt-BR labels and placeholders

    2. **Migrate `OnboardingView.tsx`** from `view_onboarding.jsx` (163 lines):
       - 4-step wizard:
         - Step 1 (Carteira): "Configure sua carteira de pacientes" — shows patient count, target audience
         - Step 2 (Plano): "Escolha seu plano" — shows 3 pricing cards (Iniciante, Profissional, Ilimitado) with feature lists
         - Step 3 (Convite): "Convide seus pacientes" — share link or WhatsApp direct invite
         - Step 4 (Pronto): "Tudo pronto!" — success confirmation with CTA to dashboard
       - Progress bar showing 1/4, 2/4, 3/4, 4/4
       - "Avançar" / "Voltar" navigation buttons
       - On step 4 completion: call `useAuthStore().login()` → `/home` (redirect to dashboard)
       - Step-specific illustrations or icons
       - Responsive layout (single column on mobile)
       - Use `onboard-*` CSS classes from globals.css or Tailwind equivalents

    **All pt-BR text must match prototype exactly.** This means:
    - Button labels: "Começar agora", "Entrar", "Cadastrar", "Avançar", "Voltar", "Concluir"
    - Form labels: "E-mail", "Senha", "Confirmar senha", "Nome completo", "CRN", "Especialidade"
    - Onboarding text: "Configure sua carteira", "Escolha seu plano", "Convide seus pacientes", "Tudo pronto!"
    - Pricing: "Iniciante R$99,99/mês", "Profissional R$149,99/mês", "Ilimitado R$199,99/mês"
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && echo "Signup and Onboarding views compile"</automated>
  </verify>
  <done>SignupView has 2-step form with validation matching prototype. OnboardingView has 4-step wizard matching prototype flow. Both navigate correctly via React Router and auth store. All text in pt-BR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side forms | Login/signup forms are UI only — no real auth API calls yet (Phase 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | S | SignupView | accept | Mock signup only — real validation and password hashing in Phase 3 |
| T-02-05 | I | form inputs | accept | Client-side validation is cosmetic until Phase 3 adds server-side |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- Landing page renders hero, features (6 cards), pricing (3 plans), footer
- Login form shows email/password with validation
- Signup form has 2 steps with form state management
- Onboarding has 4 steps with progress bar
- All navigation uses React Router `<Link>` or `useNavigate()`
- No `window.` references or global component patterns
- Pricing values match prototype: R$99,99, R$149,99, R$199,99
- All text is pt-BR
</verification>

<success_criteria>
- LandingView fully migrated with hero, features, pricing, and footer matching prototype
- LoginView with email/password form and validation
- SignupView with 2-step wizard matching prototype
- OnboardingView with 4 steps (carteira, plano, convite, pronto) matching prototype
- All views use React Router for navigation and Zustand auth store
- Light and dark themes render correctly on all views
- TypeScript compilation passes
- No window globals — all ES module imports
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-migration/02-03-SUMMARY.md`
</output>