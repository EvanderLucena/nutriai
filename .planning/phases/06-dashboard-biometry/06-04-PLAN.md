---
phase: 06-dashboard-biometry
plan: 04
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - backend/src/main/java/com/nutriai/api/service/PatientService.java
  - backend/src/main/java/com/nutriai/api/service/MealPlanService.java
  - backend/src/test/java/com/nutriai/api/service/PatientServiceTest.java
  - backend/src/test/java/com/nutriai/api/service/MealPlanServiceTest.java
autonomous: true
requirements:
  - BIO-03
user_setup: []
must_haves:
  truths:
    - "Episode lifecycle events are emitted at activation, deactivation, and plan creation seams"
    - "History timeline shows more than just biometry rows — it includes plan creation and episode open/close events"
    - "Events are persisted via EpisodeHistoryEventRepository, giving closed-cycle snapshots a real source"
  artifacts:
    - path: "backend/src/main/java/com/nutriai/api/service/PatientService.java"
      provides: "Lifecycle event emission on episode open/close"
      contains: "EpisodeHistoryEvent"
    - path: "backend/src/main/java/com/nutriai/api/service/MealPlanService.java"
      provides: "Lifecycle event emission on default plan creation"
      contains: "EpisodeHistoryEvent"
  key_links:
    - from: "backend/src/main/java/com/nutriai/api/service/PatientService.java"
      to: "backend/src/main/java/com/nutriai/api/repository/EpisodeHistoryEventRepository.java"
      via: "Persist EPISODE_OPENED and EPISODE_CLOSED events"
      pattern: "save"
    - from: "backend/src/main/java/com/nutriai/api/service/MealPlanService.java"
      to: "backend/src/main/java/com/nutriai/api/repository/EpisodeHistoryEventRepository.java"
      via: "Persist PLAN_CREATED events"
      pattern: "save"
---

<objective>
Wire lifecycle event emission into the existing seams that shape past-period context: episode open/close and plan creation.

Purpose: Without these events, closed-cycle history snapshots in Histórico would only show biometry rows. This plan ensures that episode and plan lifecycle events also appear in the timeline, giving nutritionists a complete picture of what happened in each closed period.
Output: Modified PatientService and MealPlanService persisting EpisodeHistoryEvent rows at lifecycle seams, plus updated tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-biometry/06-CONTEXT.md
@.planning/phases/06-dashboard-biometry/PATTERNS.md
@.planning/phases/06-dashboard-biometry/06-01-SUMMARY.md
@.planning/phases/06-dashboard-biometry/06-02-SUMMARY.md
@backend/src/main/java/com/nutriai/api/service/PatientService.java
@backend/src/main/java/com/nutriai/api/service/MealPlanService.java
@backend/src/main/java/com/nutriai/api/model/EpisodeHistoryEvent.java
@backend/src/main/java/com/nutriai/api/repository/EpisodeHistoryEventRepository.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit episode lifecycle events in PatientService</name>
  <files>
    backend/src/main/java/com/nutriai/api/service/PatientService.java
    backend/src/test/java/com/nutriai/api/service/PatientServiceTest.java
  </files>
  <read_first>
    backend/src/main/java/com/nutriai/api/service/PatientService.java
    backend/src/main/java/com/nutriai/api/model/EpisodeHistoryEvent.java
    backend/src/main/java/com/nutriai/api/repository/EpisodeHistoryEventRepository.java
  </read_first>
  <action>
    Inject `EpisodeHistoryEventRepository` into `PatientService`.

    In the patient activation flow (where a new episode is created):
    - After episode save, persist an `EPISODE_OPENED` event with `eventAt = episode.startDate`, title like `"Período iniciado"`, description referencing the patient activation.

    In the patient deactivation flow (where the active episode is closed):
    - After episode close, persist an `EPISODE_CLOSED` event with `eventAt = now`, title like `"Período encerrado"`, description referencing the deactivation.

    Ensure both paths use the nutritionist ID from the episode for scoping.

    Add tests to `PatientServiceTest` proving:
    - activation creates an `EPISODE_OPENED` event tied to the new episode
    - deactivation creates an `EPISODE_CLOSED` event tied to the closed episode
    - reactivate creates another `EPISODE_OPENED` event for the new episode
  </action>
  <verify>cd backend && ./gradlew test --tests "com.nutriai.api.service.PatientServiceTest"</verify>
  <acceptance_criteria>
    - PatientService persists EPISODE_OPENED on activation
    - PatientService persists EPISODE_CLOSED on deactivation
    - Reactivation persists a new EPISODE_OPENED event
    - Tests prove lifecycle events appear for the correct episode
  </acceptance_criteria>
  <done>Episode open/close events persist in the cycle-history timeline source.</done>
</task>

<task type="auto">
  <name>Task 2: Emit plan creation events in MealPlanService</name>
  <files>
    backend/src/main/java/com/nutriai/api/service/MealPlanService.java
    backend/src/test/java/com/nutriai/api/service/MealPlanServiceTest.java
  </files>
  <read_first>
    backend/src/main/java/com/nutriai/api/service/MealPlanService.java
    backend/src/main/java/com/nutriai/api/model/EpisodeHistoryEvent.java
  </read_first>
  <action>
    Inject `EpisodeHistoryEventRepository` into `MealPlanService`.

    In the default plan creation flow (the auto-created base plan when an episode is opened):
    - After plan save, persist a `PLAN_CREATED` event with `eventAt = now`, title like `"Plano alimentar criado"`, description referencing the plan name or default structure.

    Add tests to `MealPlanServiceTest` proving:
    - auto-plan creation persists a `PLAN_CREATED` event for the episode
    - event is tied to the correct episode and nutritionist
  </action>
  <verify>cd backend && ./gradlew test --tests "com.nutriai.api.service.MealPlanServiceTest"</verify>
  <acceptance_criteria>
    - MealPlanService persists PLAN_CREATED on default plan creation
    - Tests prove the event is tied to the correct episode
  </acceptance_criteria>
  <done>Closed-cycle history is backed by persisted lifecycle events rather than inferred mock data.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew compileJava`
- `cd backend && ./gradlew test --tests "com.nutriai.api.service.PatientServiceTest" --tests "com.nutriai.api.service.MealPlanServiceTest"`
- episode open/close events are persisted on activation/deactivation
- plan creation events are persisted on base plan auto-creation
- all existing tests continue to pass
</verification>

<success_criteria>
- Episode lifecycle events emitted from PatientService
- Plan creation events emitted from MealPlanService
- Existing tests still pass
- History timeline shows more than just biometry rows
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-biometry/06-04-SUMMARY.md`
</output>