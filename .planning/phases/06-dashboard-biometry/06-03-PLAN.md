---
phase: 06-dashboard-biometry
plan: 03
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - frontend/src/api/dashboard.ts
  - frontend/src/api/biometry.ts
  - frontend/src/stores/biometryStore.ts
  - frontend/src/stores/biometryStore.test.ts
  - frontend/src/types/patient.ts
  - frontend/src/views/HomeView.tsx
  - frontend/src/views/HomeView.test.tsx
  - frontend/src/views/PatientView.tsx
  - frontend/src/views/PatientView.test.tsx
  - frontend/src/components/patient/NewBiometryModal.tsx
  - frontend/src/components/patient/StatusReviewModal.tsx
  - frontend/src/components/patient/index.ts
  - frontend/playwright.config.ts
  - frontend/e2e/biometry-dashboard.spec.ts
autonomous: true
requirements:
  - BIO-01
  - BIO-02
  - BIO-03
user_setup: []
must_haves:
  truths:
    - "HomeView renders real dashboard KPIs and recent evaluations with no WhatsApp activity block"
    - "Patient biometry tab saves partial assessments with required core fields, opens a manual status-review flow after save, and renders real evolution charts"
    - "Histórico shows only past episodes and renders read-only timeline/history events for opened snapshots"
    - "The new Playwright spec is included in the authenticated project and actually runs"
  artifacts:
    - path: "frontend/src/stores/biometryStore.ts"
      provides: "Dashboard, active-biometry, and archive-history query hooks"
      contains: "useDashboard"
    - path: "frontend/src/views/PatientView.tsx"
      provides: "Real-data biometry tab and archive-history snapshot rendering"
      contains: "history"
    - path: "frontend/src/components/patient/StatusReviewModal.tsx"
      provides: "Post-save manual patient status review/update flow"
      contains: "StatusReviewModal"
    - path: "frontend/playwright.config.ts"
      provides: "Authenticated project match that includes the new Phase 06 E2E spec"
      contains: "biometry-dashboard"
    - path: "frontend/e2e/biometry-dashboard.spec.ts"
      provides: "Runnable end-to-end verification for dashboard + biometry"
      contains: "test("
  key_links:
    - from: "frontend/src/stores/biometryStore.ts"
      to: "frontend/src/api/dashboard.ts"
      via: "Dashboard aggregate query wiring"
      pattern: "useDashboard"
    - from: "frontend/src/stores/biometryStore.ts"
      to: "frontend/src/api/biometry.ts"
      via: "Active biometry and archive-history query wiring"
      pattern: "usePatientHistoryEpisodes"
    - from: "frontend/e2e/biometry-dashboard.spec.ts"
      to: "frontend/playwright.config.ts"
      via: "Spec inclusion in authenticated Playwright project"
      pattern: "authenticated"
---

<objective>
Wire the Phase 06 frontend to the backend contracts and add runnable verification for dashboard, active biometry, and archive history.

Purpose: This plan removes the remaining dashboard/biometry mocks, exposes the closed-cycle archive UX, and ensures the new E2E path actually executes under the current Playwright project setup.
Output: Dashboard/biometry API modules, biometry store, rewired HomeView and PatientView flows, modal save flow, updated Playwright config, view/store tests, and a runnable E2E spec.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-biometry/06-CONTEXT.md
@.planning/phases/06-dashboard-biometry/06-UI-SPEC.md
@.planning/phases/06-dashboard-biometry/06-RESEARCH.md
@.planning/phases/06-dashboard-biometry/PATTERNS.md
@.planning/phases/06-dashboard-biometry/06-01-SUMMARY.md
@.planning/phases/06-dashboard-biometry/06-02-SUMMARY.md
@frontend/src/api/client.ts
@frontend/src/stores/patientStore.ts
@frontend/src/views/HomeView.tsx
@frontend/src/views/PatientView.tsx
@frontend/src/components/patient/NewBiometryModal.tsx
@frontend/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 06 API modules, store hooks, and types</name>
  <files>
    frontend/src/api/dashboard.ts
    frontend/src/api/biometry.ts
    frontend/src/stores/biometryStore.ts
    frontend/src/stores/biometryStore.test.ts
    frontend/src/types/patient.ts
  </files>
  <read_first>
    frontend/src/api/plans.ts
    frontend/src/stores/patientStore.ts
    frontend/src/stores/planStore.ts
    frontend/src/types/patient.ts
  </read_first>
  <action>
    Create dashboard/biometry API modules that unwrap `response.data.data` exactly like existing patient/plan modules. Add a `biometryStore.ts` with Zustand UI state and TanStack Query hooks for dashboard, active biometry, history list, and history snapshot retrieval. Update `types/patient.ts` with DTOs for dashboard, assessments, history episodes, and timeline events while preserving existing mapped UI types.
  </action>
  <verify>cd frontend && npx tsc --noEmit && npm test -- --run biometryStore</verify>
  <acceptance_criteria>
    - API modules unwrap the standard envelope
    - biometry store exports dashboard, active-biometry, and archive-history hooks
    - store tests cover local UI state and mutation invalidation/error behavior
  </acceptance_criteria>
  <done>Phase 06 frontend data layer exists and follows the repo's View -> Store -> API split.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire dashboard, active biometry, and archive history UI</name>
  <files>
    frontend/src/views/HomeView.tsx
    frontend/src/views/PatientView.tsx
    frontend/src/components/patient/NewBiometryModal.tsx
    frontend/src/components/patient/StatusReviewModal.tsx
    frontend/src/components/patient/index.ts
  </files>
  <read_first>
    frontend/src/views/HomeView.tsx
    frontend/src/views/PatientView.tsx
    frontend/src/components/patient/NewBiometryModal.tsx
    frontend/src/components/patient/MultiLineChart.tsx
    frontend/src/components/viz/LineChart.tsx
    .planning/phases/06-dashboard-biometry/06-UI-SPEC.md
  </read_first>
  <action>
    Remove the WhatsApp activity block from `HomeView.tsx` and populate the KPI row plus recent-evaluations surface from `useDashboard()`.

    In `PatientView.tsx`:
    - wire `Biometria` to active-episode data from `usePatientBiometry(patientId)`
    - keep charting on existing `LineChart`/`MultiLineChart`
    - wire `Histórico` to show only closed episodes from `usePatientHistoryEpisodes(patientId)`
    - when a closed episode is opened, render a read-only snapshot that includes plan context, assessment list, and `timelineEvents`

    Update `NewBiometryModal.tsx` into a real save flow that enforces date + weight + body fat, leaves the remaining fields optional, and refreshes dashboard + patient biometry on success. Keep copy and grouping consistent with the current modal.

    After a successful assessment save, immediately open a dedicated manual status-review flow (`StatusReviewModal` or equivalent in the patient component set) instead of silently closing the workflow. That review flow must:
    - show the current patient status
    - let the nutritionist confirm or change status manually
    - submit through the existing patient update path/store mutation
    - return the user to the refreshed active biometry view after save/skip

    This is a locked discuss decision and must be treated as part of the main happy path, not as an optional enhancement.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - HomeView no longer renders the WhatsApp activity card
    - HomeView shows KPI and recent evaluation data from the backend
    - Biometria uses active-episode real data
    - successful biometry save opens a manual status-review flow wired to the existing patient update path
    - Histórico excludes the active episode and renders read-only timeline/history events for selected snapshots
  </acceptance_criteria>
  <done>Phase 06 UI surfaces operate on real data while preserving the current visual system.</done>
</task>

<task type="auto">
  <name>Task 3: Add runnable tests and Playwright coverage for Phase 06</name>
  <files>
    frontend/src/views/HomeView.test.tsx
    frontend/src/views/PatientView.test.tsx
    frontend/playwright.config.ts
    frontend/e2e/biometry-dashboard.spec.ts
  </files>
  <read_first>
    frontend/src/views/PlansView.test.tsx
    frontend/e2e/patient-management.spec.ts
    frontend/playwright.config.ts
  </read_first>
  <action>
    Add view tests that mock the new Phase 06 hooks and cover:
    - dashboard KPI/recent-evaluation rendering
    - active biometry populated and empty states
    - post-save status-review prompt and manual status update path
    - archive history list showing closed episodes only
    - read-only timeline/history rendering for a closed cycle

    Update `frontend/playwright.config.ts` so the authenticated project matches the new spec file, either by explicitly adding `biometry-dashboard` to the regex or by switching the authenticated project match to include the new suite safely.

    Create `frontend/e2e/biometry-dashboard.spec.ts` covering one end-to-end path: save an assessment with the minimum required fields, verify the patient biometry view updates, and verify dashboard data reflects the persisted assessment. Keep assertions hard and runnable under the authenticated project.
  </action>
  <verify>cd frontend && npm test -- --run HomeView --run PatientView && npm run test:e2e -- biometry-dashboard.spec.ts</verify>
  <acceptance_criteria>
    - Playwright config includes the new spec in the authenticated project
    - view tests cover dashboard, biometry, and archive history behaviors
    - tests cover the post-save manual status-review flow
    - E2E spec is runnable by the configured project and verifies save + dashboard reflection
  </acceptance_criteria>
  <done>Phase 06 verification is real, runnable, and aligned with the current Playwright project setup.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit`
- `cd frontend && npm test -- --run biometryStore --run HomeView --run PatientView`
- `cd frontend && npm run test:e2e -- biometry-dashboard.spec.ts`
- HomeView has no WhatsApp card and renders real KPI/recent-evaluation data
- successful assessment save opens the manual patient status-review flow
- Histórico renders closed-cycle timeline/history events read-only
</verification>

<success_criteria>
- Frontend is fully wired to Phase 06 backend contracts
- Closed-cycle history includes timeline/history, not just plan + assessments
- Playwright config actually runs the new spec
- TypeScript, frontend tests, and the new E2E path pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-biometry/06-03-SUMMARY.md`
</output>
