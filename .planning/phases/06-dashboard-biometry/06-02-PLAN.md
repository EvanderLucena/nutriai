---
phase: 06-dashboard-biometry
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - backend/src/main/java/com/nutriai/api/dto/biometry/CreateBiometryAssessmentRequest.java
  - backend/src/main/java/com/nutriai/api/dto/biometry/UpdateBiometryAssessmentRequest.java
  - backend/src/main/java/com/nutriai/api/dto/biometry/BiometryAssessmentResponse.java
  - backend/src/main/java/com/nutriai/api/service/BiometryService.java
  - backend/src/main/java/com/nutriai/api/controller/BiometryController.java
  - backend/src/test/java/com/nutriai/api/service/BiometryServiceTest.java
  - backend/src/test/java/com/nutriai/api/controller/BiometryControllerTest.java
autonomous: true
requirements:
  - BIO-01
  - BIO-03
user_setup: []
must_haves:
  truths:
    - "Biometric assessments can only be created/updated on patients with an active episode"
    - "Assessment save requires date + weight + body fat percentage; remaining fields are optional"
    - "All biometry reads and writes are scoped to the authenticated nutritionist"
    - "Creating or updating an assessment persists an episode_history_event for cycle history"
  artifacts:
    - path: "backend/src/main/java/com/nutriai/api/service/BiometryService.java"
      provides: "Active-episode assessment CRUD and history-event emission"
      contains: "class BiometryService"
    - path: "backend/src/main/java/com/nutriai/api/controller/BiometryController.java"
      provides: "Biometry CRUD endpoints under /api/v1/patients/{patientId}"
      contains: "@PreAuthorize"
  key_links:
    - from: "backend/src/main/java/com/nutriai/api/service/BiometryService.java"
      to: "backend/src/main/java/com/nutriai/api/repository/EpisodeRepository.java"
      via: "Active episode lookup for create/update"
      pattern: "findTopByPatientIdAndEndDateIsNullOrderByStartDateDesc"
    - from: "backend/src/main/java/com/nutriai/api/service/BiometryService.java"
      to: "backend/src/main/java/com/nutriai/api/repository/EpisodeHistoryEventRepository.java"
      via: "Persist cycle events on assessment create/update"
      pattern: "save"
---

<objective>
Implement active-episode biometry CRUD service, controller, DTOs, and tests on top of the schema and repositories from 06-01.

Purpose: This plan delivers the working backend API for biometry assessments, including required-field validation, active-episode binding, nutritionist scoping, and history-event emission.
Output: BiometryService, BiometryController, request/response DTOs, and service/controller test suites.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-biometry/06-CONTEXT.md
@.planning/phases/06-dashboard-biometry/06-RESEARCH.md
@.planning/phases/06-dashboard-biometry/PATTERNS.md
@.planning/phases/06-dashboard-biometry/06-01-SUMMARY.md
@backend/src/main/java/com/nutriai/api/controller/PatientController.java
@backend/src/main/java/com/nutriai/api/controller/PlanController.java
@backend/src/main/java/com/nutriai/api/service/PatientService.java
@backend/src/main/java/com/nutriai/api/dto/ApiResponse.java
@backend/src/main/java/com/nutriai/api/model/BiometryAssessment.java
@backend/src/main/java/com/nutriai/api/model/EpisodeHistoryEvent.java
@backend/src/main/java/com/nutriai/api/repository/BiometryAssessmentRepository.java
@backend/src/main/java/com/nutriai/api/repository/EpisodeHistoryEventRepository.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create biometry request/response DTOs</name>
  <files>
    backend/src/main/java/com/nutriai/api/dto/biometry/CreateBiometryAssessmentRequest.java
    backend/src/main/java/com/nutriai/api/dto/biometry/UpdateBiometryAssessmentRequest.java
    backend/src/main/java/com/nutriai/api/dto/biometry/BiometryAssessmentResponse.java
  </files>
  <read_first>
    backend/src/main/java/com/nutriai/api/dto/patient/PatientResponse.java
    backend/src/main/java/com/nutriai/api/dto/ApiResponse.java
  </read_first>
  <action>
    Create three DTOs in a `dto/biometry` package:

    **CreateBiometryAssessmentRequest:**
    - `assessmentDate` @NotNull LocalDate
    - `weight` @NotNull @DecimalMin BigDecimal
    - `bodyFatPercent` @NotNull @DecimalMin BigDecimal
    - Optional: `leanMassKg`, `waterPercent`, `visceralFatLevel` (Integer), `bmrKcal` (Integer), `device` (String), `notes` (String)
    - Optional nested: `skinfolds` List<SkinfoldEntry> (measureKey, valueMm, sortOrder), `perimetry` List<PerimetryEntry> (measureKey, valueCm, sortOrder)

    **UpdateBiometryAssessmentRequest:**
    - All fields optional (null = no change). Use `@NotNull` only on the assessment path variable.

    **BiometryAssessmentResponse:**
    - `id`, `episodeId`, `assessmentDate`, `weight`, `bodyFatPercent`, plus all optional fields
    - Nested `skinfolds` list and `perimetry` list with their key/value/sort
    - Follow the field naming and structure of `PatientResponse` for consistency.
  </action>
  <verify>cd backend && ./gradlew compileJava</verify>
  <acceptance_criteria>
    - DTOs compile and follow the existing dto package conventions
    - CreateBiometryAssessmentRequest enforces required fields with validation annotations
    - Response includes nested skinfolds and perimetry lists
  </acceptance_criteria>
  <done>Biometry DTOs enforce the locked save contract and expose nested optional sections.</done>
</task>

<task type="auto">
  <name>Task 2: Implement BiometryService and BiometryController</name>
  <files>
    backend/src/main/java/com/nutriai/api/service/BiometryService.java
    backend/src/main/java/com/nutriai/api/controller/BiometryController.java
  </files>
  <read_first>
    backend/src/main/java/com/nutriai/api/service/PatientService.java
    backend/src/main/java/com/nutriai/api/service/MealPlanService.java
    backend/src/main/java/com/nutriai/api/controller/PatientController.java
    backend/src/main/java/com/nutriai/api/controller/PlanController.java
  </read_first>
  <action>
    Implement `BiometryService` and `BiometryController`:

    **BiometryService** (transactional):
    - `createAssessment(nutritionistId, patientId, request)`: find patient by id scoped to nutritionist → find active episode → create assessment with required/optional fields → save skinfolds/perimetry if present → emit `EPISODE_BIOMETRY_CREATED` history event → return response
    - `updateAssessment(nutritionistId,patientId, assessmentId, request)`: find assessment scoped to nutritionist → update non-null fields → replace skinfolds/perimetry if provided → emit `EPISODE_BIOMETRY_UPDATED` history event → return response
    - `listAssessments(nutritionistId, patientId)`: find patient scoped → find active episode → list assessments ordered by date
    - Throw if no active episode (400 Bad Request with message)
    - Patient ownership check: if patient not found for nutritionist, throw 404-equivalent

    **BiometryController** (`@RestController`, `@RequestMapping("/api/v1/patients/{patientId}/biometry")`, `@PreAuthorize("hasRole('NUTRITIONIST')")`):
    - `POST /` → create
    - `PATCH /{assessmentId}` → update
    - `GET /` → list for active episode
    - Extract nutritionistId via `NutritionistAccess.getCurrentNutritionistId()`
    - Wrap all responses in `ApiResponse.ok(...)`
  </action>
  <verify>cd backend && ./gradlew compileJava</verify>
  <acceptance_criteria>
    - biometry controller path exists under `/api/v1/patients/{patientId}/biometry`
    - controller has `@PreAuthorize("hasRole('NUTRITIONIST')")`
    - POST requires date + weight + body fat; allows optional sections
    - PATCH allows null-for-no-change
    - create/update write `episode_history_event` rows
    - no active episode → 400 error
    - cross-nutritionist access → 404
  </acceptance_criteria>
  <done>Backend supports active-episode assessment CRUD with validation, scoping, and history-event emission.</done>
</task>

<task type="auto">
  <name>Task 3: Add service and controller tests for validation and tenant isolation</name>
  <files>
    backend/src/test/java/com/nutriai/api/service/BiometryServiceTest.java
    backend/src/test/java/com/nutriai/api/controller/BiometryControllerTest.java
  </files>
  <read_first>
    backend/src/test/java/com/nutriai/api/service/PatientServiceTest.java
    backend/src/test/java/com/nutriai/api/controller/PatientControllerTest.java
  </read_first>
  <action>
    Add service/controller tests proving:

    **BiometryServiceTest** (Mockito unit tests):
    - create assessment succeeds with required fields only (no skinfolds/perimetry)
    - create assessment succeeds with all optional fields
    - create fails when no active episode exists
    - create fails when patient does not belong to nutritionist
    - update assessment modifies only provided fields
    - create and update emit history events for the same episode

    **BiometryControllerTest** (`@SpringBootTest + @AutoConfigureMockMvc + @Transactional`):
    - POST returns 200 with ApiResponse envelope and assessment response
    - POST with missing required fields returns 400
    - PATCH updates assessment and returns updated response
    - GET returns assessments for active episode
    - cross-nutritionist access returns 404
    - no active episode returns 400
  </action>
  <verify>cd backend && ./gradlew test --tests "com.nutriai.api.service.BiometryServiceTest" --tests "com.nutriai.api.controller.BiometryControllerTest"</verify>
  <acceptance_criteria>
    - service tests cover validation, active-episode requirement, scope isolation, and history-event emission
    - controller tests cover envelope shape, required-field validation, and cross-tenant 404
    - all new tests pass
  </acceptance_criteria>
  <done>Core active-biometry rules are test-protected before dashboard/history work begins.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew compileJava`
- `cd backend && ./gradlew test --tests "com.nutriai.api.service.BiometryServiceTest" --tests "com.nutriai.api.controller.BiometryControllerTest"`
- active biometry writes are tied to the active episode and generate cycle-history events
- required fields enforced, cross-tenant access blocked
</verification>

<success_criteria>
- Active biometry CRUD is implemented with DTOs, service, and controller
- Required-field and episode-binding validation enforced
- History events emitted on create/update
- Tests cover validation, isolation, and history emission
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-biometry/06-02-SUMMARY.md`
</output>