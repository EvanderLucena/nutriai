---
phase: 06-dashboard-biometry
plan: 05
type: execute
wave: 4
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - frontend/src/api/dashboard.ts
  - frontend/src/api/biometry.ts
  - frontend/src/stores/clinicalStore.ts
  - frontend/src/stores/clinicalStore.test.ts
  - frontend/src/types/patient.ts
  - frontend/src/views/HomeView.tsx
  - frontend/src/views/HomeView.test.tsx
  - frontend/src/views/PatientView.tsx
  - frontend/src/views/PatientView.test.tsx
  - frontend/src/components/patient/NewBiometryModal.tsx
  - frontend/src/components/patient/StatusReviewModal.tsx
  - frontend/src/components/patient/index.ts
  - frontend/playwright.config.ts
  - frontend/e2e/biometry-dashboard.spec.ts
autonomous: true
requirements:
  - BIO-01
  - BIO-02
  - BIO-03
user_setup: []
must_haves:
  truths:
    - "HomeView renders real dashboard KPIs and recent evaluations with no WhatsApp activity block and no WhatsApp-referencing KPI label"
    - "Patient biometry tab saves partial assessments with required core fields (date + weight + body fat), opens a manual status-review flow after save, and renders real evolution charts with graceful empty/single-point states"
    - "Histórico shows only past episodes and renders read-only timeline/history events for opened snapshots, with honest sparse-state labels for episodes with no biometry"
    - "NewBiometryModal is a fully functional form with controlled useState, validation, API call, and post-save flow — not a visual shell"
    - "ANA mock import is removed for biometry/skinfolds/perimetry fields; other ANA fields (macrosToday, weekAdherence, weekMacroFill, timeline, aiSummary) remain as fallback until their phases"
    - "Dashboard hooks live in clinicalStore.ts (not biometryStore.ts) to reflect both dashboard and biometry concerns"
    - "The new Playwright spec is included in the authenticated project and actually runs"
  artifacts:
    - path: "frontend/src/stores/clinicalStore.ts"
      provides: "Dashboard, active-biometry, and archive-history query hooks"
      contains: "useDashboard"
    - path: "frontend/src/views/PatientView.tsx"
      provides: "Real-data biometry tab and archive-history snapshot rendering"
      contains: "history"
    - path: "frontend/src/components/patient/StatusReviewModal.tsx"
      provides: "Post-save manual patient status review/update flow"
      contains: "StatusReviewModal"
    - path: "frontend/src/components/patient/NewBiometryModal.tsx"
      provides: "Fully functional biometry form with validation and mutation"
      contains: "useState"
    - path: "frontend/playwright.config.ts"
      provides: "Authenticated project match that includes the new Phase 06 E2E spec"
      contains: "biometry-dashboard"
    - path: "frontend/e2e/biometry-dashboard.spec.ts"
      provides: "Runnable end-to-end verification for dashboard + biometry"
      contains: "test("
  key_links:
    - from: "frontend/src/stores/clinicalStore.ts"
      to: "frontend/src/api/dashboard.ts"
      via: "Dashboard aggregate query wiring"
      pattern: "useDashboard"
    - from: "frontend/src/stores/clinicalStore.ts"
      to: "frontend/src/api/biometry.ts"
      via: "Active biometry and archive-history query wiring"
      pattern: "usePatientHistoryEpisodes"
---

<objective>
Wire the Phase 06 frontend to the backend contracts, remove mock data, add the StatusReviewModal, and add runnable verification.

Purpose: This plan removes the remaining dashboard/biometry mocks, exposes the closed-cycle archive UX, adds the post-save status review flow (locked decision D-08), and ensures the new E2E path actually executes.
Output: Dashboard/biometry API modules, clinical store, rewired HomeView and PatientView flows, functional NewBiometryModal, StatusReviewModal, updated Playwright config, view/store tests, and a runnable E2E spec.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-biometry/06-CONTEXT.md
@.planning/phases/06-dashboard-biometry/06-UI-SPEC.md
@.planning/phases/06-dashboard-biometry/06-RESEARCH.md
@.planning/phases/06-dashboard-biometry/PATTERNS.md
@.planning/phases/06-dashboard-biometry/06-01-SUMMARY.md
@.planning/phases/06-dashboard-biometry/06-02-SUMMARY.md
@.planning/phases/06-dashboard-biometry/06-03-SUMMARY.md
@.planning/phases/06-dashboard-biometry/06-04-SUMMARY.md
@frontend/src/api/client.ts
@frontend/src/api/plans.ts
@frontend/src/stores/patientStore.ts
@frontend/src/stores/planStore.ts
@frontend/src/types/patient.ts
@frontend/src/views/HomeView.tsx
@frontend/src/views/PatientView.tsx
@frontend/src/components/patient/NewBiometryModal.tsx
@frontend/src/components/patient/MultiLineChart.tsx
@frontend/src/components/viz/LineChart.tsx
@frontend/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 06 API modules, clinical store, and types</name>
  <files>
    frontend/src/api/dashboard.ts
    frontend/src/api/biometry.ts
    frontend/src/stores/clinicalStore.ts
    frontend/src/stores/clinicalStore.test.ts
    frontend/src/types/patient.ts
  </files>
  <read_first>
    frontend/src/api/plans.ts
    frontend/src/stores/patientStore.ts
    frontend/src/stores/planStore.ts
    frontend/src/types/patient.ts
  </read_first>
  <action>
    Create dashboard/biometry API modules that unwrap `response.data.data` exactly like existing patient/plan modules.

    **dashboard.ts**: `getDashboard()` → GET `/api/v1/dashboard`, return `response.data.data`

    **biometry.ts**:
    - `listBiometryAssessments(patientId)` → GET `/api/v1/patients/${patientId}/biometry`
    - `createBiometryAssessment(patientId, data)` → POST same path
    - `updateBiometryAssessment(patientId, assessmentId, data)` → PATCH `.../biometry/${assessmentId}`
    - `listHistoryEpisodes(patientId)` → GET `.../history/episodes`
    - `getHistorySnapshot(patientId, episodeId)` → GET `.../history/episodes/${episodeId}`

    **clinicalStore.ts** (not biometryStore.ts — naming covers both dashboard and biometry):
    - Zustand UI state: `selectedHistoryEpisodeId`, `selectedChartMetric`, `biometryModalOpen`
    - TanStack Query hooks: `useDashboard()`, `usePatientBiometry(patientId)`, `useCreateBiometry(patientId)`, `useUpdateBiometry(patientId)`, `usePatientHistoryEpisodes(patientId)`, `useHistoricalEpisode(patientId, episodeId)`
    - Mutation invalidation keys: `['dashboard']`, `['patient-biometry', patientId]`, `['patient-history', patientId]`

    **types/patient.ts**: Add DTOs for dashboard, assessments, history episodes, and timeline events while preserving existing mapped UI types. New types:
    - `DashboardData`, `DashboardKPIs`, `RecentEvaluation`
    - `BiometryAssessmentDTO`, `SkinfoldEntry`, `PerimetryEntry`
    - `HistoryEpisodeListItem`, `HistorySnapshot`, `TimelineEvent`
    - Map functions from API shape to UI shape as needed.

    Add store tests covering local UI state transitions and mutation invalidation/error behavior.
  </action>
  <verify>cd frontend && npx tsc --noEmit && npm test -- --run clinicalStore</verify>
  <acceptance_criteria>
    - API modules unwrap the standard envelope (`response.data.data`)
    - clinicalStore exports useDashboard, usePatientBiometry, usePatientHistoryEpisodes, useHistoricalEpisode hooks
    - store tests cover local UI state and mutation invalidation
    - types include DTOs for dashboard, assessments, history episodes, and timeline events
  </acceptance_criteria>
  <done>Phase 06 frontend data layer exists in clinicalStore.ts following the repo's View → Store → API split.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire dashboard, active biometry, StatusReviewModal, and archive history UI</name>
  <files>
    frontend/src/views/HomeView.tsx
    frontend/src/views/PatientView.tsx
    frontend/src/components/patient/NewBiometryModal.tsx
    frontend/src/components/patient/StatusReviewModal.tsx
    frontend/src/components/patient/index.ts
  </files>
  <read_first>
    frontend/src/views/HomeView.tsx
    frontend/src/views/PatientView.tsx
    frontend/src/components/patient/NewBiometryModal.tsx
    frontend/src/components/patient/MultiLineChart.tsx
    frontend/src/components/viz/LineChart.tsx
    .planning/phases/06-dashboard-biometry/06-UI-SPEC.md
  </read_first>
  <action>
    **HomeView.tsx** — Remove the WhatsApp activity block entirely:
    - Delete the "Refeições registradas" KPI (`label="Refeições registradas" sub="via WhatsApp"`) — replace with a KPI from useDashboard() such as `assessedInLast30Days`
    - Delete the entire "Refeições reportadas · hoje" activity card (the EXTRAÍDO VIA WHATSAPP / AO VIVO block)
    - Delete the mock `events` array construction
    - Replace KPI row with real data from `useDashboard()`: activePatients, attentionPatients, criticalPatients, averageAdherence, recentEvaluations

    **PatientView.tsx** — Rewire Biometria and Histórico tabs:
    - Wire `Biometria` to active-episode data from `usePatientBiometry(patientId)`
    - Map API response `BiometryAssessmentDTO[]` into chart-compatible format for existing `LineChart`/`MultiLineChart`
    - Handle empty state (0 assessments) and single-point state (1 assessment) gracefully — show "Nenhuma avaliação registrada ainda" message per UI-SPEC copy, or render single-point on chart if one exists
    - Wire `Histórico` to show only closed episodes from `usePatientHistoryEpisodes(patientId)`
    - When a closed episode is opened, render a read-only snapshot via `useHistoricalEpisode(patientId, episodeId)` that includes plan context, assessment list, and `timelineEvents`
    - For sparse episodes, show honest labels: "Sem avaliação registrada no período" when `hasBiometry === false`
    - Remove `ANA` import fallback for biometry, skinfolds, and perimetry fields. Keep ANA fallback for macrosToday, weekAdherence, weekMacroFill, timeline, and aiSummary (those are NOT in scope for Phase 6)
    - Replace mock `HistoryDay[]` data structure entirely with real episode-based history

    **NewBiometryModal.tsx** — Convert from visual shell to functional form:
    - Add controlled form state via `useState` for all fields
    - Add validation: date + weight + bodyFat are required; show error state on missing required fields
    - Submit calls `useCreateBiometry(patientId)` mutation
    - On success, close form and open `StatusReviewModal`
    - Keep existing copy ("Preencha só o que tiver disponível") and field grouping

    **StatusReviewModal.tsx** — New component:
    - Shows current patient status (ontrack/warning/danger)
    - Lets nutritionist confirm or change status manually
    - Submits via existing `useUpdatePatient()` mutation from patientStore
    - After submit or skip, returns user to refreshed active biometry view
    - Props: `patientId`, `currentStatus`, `onClose`

    **index.ts** — Add `StatusReviewModal` to barrel export.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - HomeView has no WhatsApp card or WhatsApp-referencing KPI labels
    - HomeView shows KPI and recent evaluation data from useDashboard()
    - Biometria uses active-episode real data with graceful 0/1 assessment states
    - successful biometry save opens StatusReviewModal
    - StatusReviewModal connects to useUpdatePatient and allows manual status change
    - Histórico excludes the active episode and renders read-only timeline/history events
    - ANA fallback removed for biometry/skinfolds/perimetry; kept for out-of-scope fields
    - mock HistoryDay data removed entirely
  </acceptance_criteria>
  <done>Phase 06 UI surfaces operate on real data while preserving the current visual system.</done>
</task>

<task type="auto">
  <name>Task 3: Add runnable tests and Playwright coverage for Phase 06</name>
  <files>
    frontend/src/views/HomeView.test.tsx
    frontend/src/views/PatientView.test.tsx
    frontend/playwright.config.ts
    frontend/e2e/biometry-dashboard.spec.ts
  </files>
  <read_first>
    frontend/src/views/PlansView.test.tsx
    frontend/e2e/patient-management.spec.ts
    frontend/playwright.config.ts
  </read_first>
  <action>
    Add view tests that mock the Phase 06 hooks and cover:
    - dashboard KPI/recent-evaluation rendering from useDashboard()
    - active biometry populated, empty, and single-assessment states
    - post-save status-review prompt (StatusReviewModal opens after biometry create)
    - manual status update path through StatusReviewModal
    - archive history list showing closed episodes only
    - read-only timeline/history rendering for a closed cycle
    - sparse-episode labeling ("Sem avaliação registrada no período")

    Update `frontend/playwright.config.ts` to include `biometry-dashboard` in the authenticated project match pattern.

    Create `frontend/e2e/biometry-dashboard.spec.ts` covering:
    - save an assessment with minimum required fields (date + weight + body fat)
    - verify StatusReviewModal appears after save and allows status change
    - verify biometry view updates with the new assessment
    - verify dashboard KPI/recent-evaluations reflect the persisted assessment
    - navigate to Histórico tab, verify closed episodes appear
  </action>
  <verify>cd frontend && npm test -- --run HomeView --run PatientView && npm run test:e2e -- biometry-dashboard.spec.ts</verify>
  <acceptance_criteria>
    - Playwright config includes the new spec in the authenticated project
    - view tests cover dashboard, biometry, StatusReviewModal, and archive history behaviors
    - E2E spec covers assessment save, status review modal, dashboard reflection, and history tab
  </acceptance_criteria>
  <done>Phase 06 verification is real, runnable, and covers the locked post-save status review decision.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit`
- `cd frontend && npm test -- --run clinicalStore --run HomeView --run PatientView`
- `cd frontend && npm run test:e2e -- biometry-dashboard.spec.ts`
- HomeView has no WhatsApp card and renders real KPI/recent-evaluation data
- successful assessment save opens the manual patient status-review flow
- Histórico renders closed-cycle timeline/history events read-only
- empty/single-point chart states show graceful messages
</verification>

<success_criteria>
- Frontend is fully wired to Phase 06 backend contracts
- No WhatsApp references remain on the dashboard
- NewBiometryModal is a functional form, not a visual shell
- StatusReviewModal implements locked decision D-08
- Closed-cycle history includes timeline/history, not just plan + assessments
- ANA fallback removed for biometry/skinfolds/perimetry (kept for out-of-scope fields)
- Playwright config actually runs the new spec
- TypeScript, frontend tests, and the new E2E path pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-biometry/06-05-SUMMARY.md`
</output>