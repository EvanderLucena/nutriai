---
phase: 05-meal-plans-food-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/nutriai/api/model/Food.java
  - backend/src/main/java/com/nutriai/api/model/FoodPortion.java
  - backend/src/main/java/com/nutriai/api/model/MealPlan.java
  - backend/src/main/java/com/nutriai/api/model/MealSlot.java
  - backend/src/main/java/com/nutriai/api/model/MealOption.java
  - backend/src/main/java/com/nutriai/api/model/MealFood.java
  - backend/src/main/java/com/nutriai/api/model/PlanExtra.java
  - backend/src/main/java/com/nutriai/api/repository/FoodRepository.java
  - backend/src/main/java/com/nutriai/api/repository/FoodPortionRepository.java
  - backend/src/main/java/com/nutriai/api/repository/MealPlanRepository.java
  - backend/src/main/java/com/nutriai/api/repository/MealSlotRepository.java
  - backend/src/main/java/com/nutriai/api/repository/MealOptionRepository.java
  - backend/src/main/java/com/nutriai/api/repository/MealFoodRepository.java
  - backend/src/main/java/com/nutriai/api/repository/PlanExtraRepository.java
  - backend/src/main/java/com/nutriai/api/service/FoodService.java
  - backend/src/main/java/com/nutriai/api/controller/FoodController.java
  - backend/src/main/java/com/nutriai/api/dto/food/CreateFoodRequest.java
  - backend/src/main/java/com/nutriai/api/dto/food/UpdateFoodRequest.java
  - backend/src/main/java/com/nutriai/api/dto/food/FoodResponse.java
  - backend/src/main/java/com/nutriai/api/dto/food/FoodPortionDto.java
  - backend/src/main/java/com/nutriai/api/service/MealPlanService.java
  - backend/src/main/java/com/nutriai/api/controller/PlanController.java
  - backend/src/main/java/com/nutriai/api/dto/plan/PlanResponse.java
  - backend/src/main/java/com/nutriai/api/dto/plan/AddFoodItemRequest.java
  - backend/src/main/java/com/nutriai/api/dto/plan/UpdateFoodItemRequest.java
  - backend/src/main/java/com/nutriai/api/dto/plan/AddMealSlotRequest.java
  - backend/src/main/java/com/nutriai/api/dto/plan/AddOptionRequest.java
  - backend/src/main/java/com/nutriai/api/dto/plan/AddExtraRequest.java
  - backend/src/main/java/com/nutriai/api/dto/plan/UpdateExtraRequest.java
  - backend/src/main/java/com/nutriai/api/dto/plan/UpdatePlanRequest.java
  - backend/src/main/resources/db/migration/V5__create_meal_plan_and_food_catalog_tables.sql
  - backend/src/test/java/com/nutriai/api/service/FoodServiceTest.java
  - backend/src/test/java/com/nutriai/api/service/MealPlanServiceTest.java
  - backend/src/test/java/com/nutriai/api/controller/FoodControllerTest.java
  - backend/src/test/java/com/nutriai/api/controller/PlanControllerTest.java
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05]

must_haves:
  truths:
    - "Backend calculates macros from per100 data × grams / 100 and stores frozen values"
    - "Food catalog CRUD isolates data by nutritionist_id"
    - "Meal plan CRUD links to episode_id (1:1), not patient_id"
    - "Patient creation auto-creates a 6-meal template plan"
    - "Editing a food in catalog does NOT update existing plan macros (frozen)"
    - "Multiple options per meal slot and extras work via API"
  artifacts:
    - path: "backend/src/main/resources/db/migration/V5__create_meal_plan_and_food_catalog_tables.sql"
      provides: "All Phase 5 database tables"
      contains: "CREATE TABLE meal_plan"
    - path: "backend/src/main/java/com/nutriai/api/model/Food.java"
      provides: "Food entity with BASE/PRESET types"
      contains: "class Food"
    - path: "backend/src/main/java/com/nutriai/api/model/MealPlan.java"
      provides: "MealPlan entity linked to episode"
      contains: "class MealPlan"
    - path: "backend/src/main/java/com/nutriai/api/service/FoodService.java"
      provides: "Food CRUD with nutritionist isolation"
      contains: "class FoodService"
    - path: "backend/src/main/java/com/nutriai/api/service/MealPlanService.java"
      provides: "Plan CRUD, macro calculation, auto-creation"
      contains: "calculateMacros"
    - path: "backend/src/main/java/com/nutriai/api/controller/FoodController.java"
      provides: "Food REST endpoints"
      contains: "class FoodController"
    - path: "backend/src/main/java/com/nutriai/api/controller/PlanController.java"
      provides: "Plan REST endpoints"
      contains: "class PlanController"
  key_links:
    - from: "backend/src/main/java/com/nutriai/api/service/MealPlanService.java"
      to: "backend/src/main/java/com/nutriai/api/service/PatientService.java"
      via: "Plan auto-creation on patient create"
      pattern: "createDefaultPlan"
    - from: "backend/src/main/java/com/nutriai/api/service/MealPlanService.java"
      to: "backend/src/main/java/com/nutriai/api/model/Food.java"
      via: "Macro calculation from per100 data"
      pattern: "per100.*grams"
    - from: "backend/src/main/java/com/nutriai/api/controller/PlanController.java"
      to: "backend/src/main/java/com/nutriai/api/auth/NutritionistAccess.java"
      via: "Data isolation"
      pattern: "getCurrentNutritionistId"
---

<objective>
Build the complete backend for meal plans and food catalog: entities, Flyway migration, repositories, services with macro calculation, and REST controllers with data isolation.

Purpose: The backend is the foundation for the meal plan editor — all macro calculations happen here (D-20), data is isolated per nutritionist (D-08), and plans auto-create with patient episodes (D-13).
Output: 7 JPA entities, V5 migration, 6 repositories, FoodService + FoodController, MealPlanService + PlanController with all CRUD + macro calculation + auto-creation, and unit/integration tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-meal-plans-food-catalog/05-CONTEXT.md
@.planning/phases/05-meal-plans-food-catalog/05-RESEARCH.md
@.planning/phases/04-patient-management/04-01-SUMMARY.md
@.planning/phases/04-patient-management/04-02-SUMMARY.md
@backend/src/main/java/com/nutriai/api/model/Episode.java
@backend/src/main/java/com/nutriai/api/model/Patient.java
@backend/src/main/java/com/nutriai/api/model/Nutritionist.java
@backend/src/main/java/com/nutriai/api/auth/NutritionistAccess.java
@backend/src/main/java/com/nutriai/api/controller/PatientController.java
@backend/src/main/java/com/nutriai/api/service/PatientService.java
@backend/src/main/java/com/nutriai/api/dto/ApiResponse.java

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/src/main/java/com/nutriai/api/model/Episode.java:
```java
@Entity @Table(name = "episode")
public class Episode {
    @Id @GeneratedValue(strategy = GenerationType.AUTO) private UUID id;
    @Column(name = "patient_id", nullable = false) private UUID patientId;
    @Column(name = "start_date", nullable = false) private LocalDateTime startDate;
    @Column(name = "end_date") private LocalDateTime endDate;
    // @PrePersist, @PreUpdate, close(), isActive()
}
```

From backend/src/main/java/com/nutriai/api/auth/NutritionistAccess.java:
```java
public final class NutritionistAccess {
    public static UUID getCurrentNutritionistId() {
        Authentication authentication = SecurityContextHolder.getContext().getAuthentication();
        return (UUID) authentication.getPrincipal();
    }
}
```

From backend/src/main/java/com/nutriai/api/dto/ApiResponse.java:
```java
public class ApiResponse<T> {
    private boolean success;
    private T data;
    private String message;
    private int status;
    private List<FieldError> errors;
}
```

From backend/src/main/java/com/nutriai/api/controller/PatientController.java:
```java
// Pattern: @RestController, @RequestMapping("/api/v1/patients"), @PreAuthorize("hasRole('NUTRITIONIST')")
// Uses NutritionistAccess.getCurrentNutritionistId() for data isolation
// Returns ResponseEntity<ApiResponse<T>> with appropriate HTTP status codes
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create JPA entities and Flyway V5 migration</name>
  <files>
    backend/src/main/java/com/nutriai/api/model/Food.java
    backend/src/main/java/com/nutriai/api/model/FoodPortion.java
    backend/src/main/java/com/nutriai/api/model/MealPlan.java
    backend/src/main/java/com/nutriai/api/model/MealSlot.java
    backend/src/main/java/com/nutriai/api/model/MealOption.java
    backend/src/main/java/com/nutriai/api/model/MealFood.java
    backend/src/main/java/com/nutriai/api/model/PlanExtra.java
    backend/src/main/java/com/nutriai/api/repository/FoodRepository.java
    backend/src/main/java/com/nutriai/api/repository/FoodPortionRepository.java
    backend/src/main/java/com/nutriai/api/repository/MealPlanRepository.java
    backend/src/main/java/com/nutriai/api/repository/MealSlotRepository.java
    backend/src/main/java/com/nutriai/api/repository/MealOptionRepository.java
    backend/src/main/java/com/nutriai/api/repository/MealFoodRepository.java
    backend/src/main/java/com/nutriai/api/repository/PlanExtraRepository.java
    backend/src/main/resources/db/migration/V5__create_meal_plan_and_food_catalog_tables.sql
  </files>
  <read_first>
    backend/src/main/java/com/nutriai/api/model/Episode.java
    backend/src/main/java/com/nutriai/api/model/Patient.java
    backend/src/main/java/com/nutriai/api/model/Nutritionist.java
    backend/src/main/resources/db/migration/V4__create_patient_and_episode_tables.sql
  </read_first>
  <behavior>
    - Test 1: Food entity with type=BASE has per100 fields populated, preset fields null
    - Test 2: Food entity with type=PRESET has preset fields populated, per100 fields null
    - Test 3: MealPlan has unique constraint on episode_id (1:1)
    - Test 4: MealFood with foodId=null still persists (nullable FK for free-text foods)
    - Test 5: Cascading delete: removing MealSlot cascades to MealOption → MealFood
  </behavior>
  <action>
    Create the 7 JPA entities and V5 migration following the entity model in 05-RESEARCH.md:

    **Food entity** (`Food.java`):
    - `@Entity @Table(name = "food_catalog")`
    - Fields: id (UUID, AUTO), nutritionistId (UUID, NOT NULL), type (String, NOT NULL — "BASE" or "PRESET"), name (String 200, NOT NULL), category (String 50)
    - BASE-only: per100Kcal, per100Prot, per100Carb, per100Fat, per100Fiber (all BigDecimal, nullable)
    - PRESET-only: presetGrams (BigDecimal, nullable), presetKcal/presetProt/presetCarb/presetFat (BigDecimal, nullable), portionLabel (String 200, nullable)
    - usedCount (Integer, default 0)
    - `@OneToMany(mappedBy = "foodId", cascade = CascadeType.ALL, orphanRemoval = true)` for FoodPortion
    - Standard @PrePersist/@PreUpdate timestamps
    - Use @Getter @Setter @Builder @NoArgsConstructor @AllArgsConstructor (same as Episode.java)

    **FoodPortion entity** (`FoodPortion.java`):
    - `@Entity @Table(name = "food_portion")`
    - Fields: id (UUID), foodId (UUID, FK → food_catalog with ON DELETE CASCADE), name (String 100), grams (BigDecimal), sortOrder (Integer)
    - Index on food_id for quick lookup

    **MealPlan entity** (`MealPlan.java`):
    - `@Entity @Table(name = "meal_plan")`
    - Fields: id (UUID), episodeId (UUID, FK → episode, UNIQUE — D-13: 1 plan per episode), nutritionistId (UUID, FK → nutritionist), title (String 200, default "Plano alimentar"), notes (TEXT, nullable), kcalTarget/protTarget/carbTarget/fatTarget (BigDecimal)
    - `@OneToMany(mappedBy = "planId", cascade = CascadeType.ALL, orphanRemoval = true)` for MealSlot
    - `@OneToMany(mappedBy = "planId", cascade = CascadeType.ALL, orphanRemoval = true)` for PlanExtra
    - Standard timestamps

    **MealSlot entity** (`MealSlot.java`):
    - `@Entity @Table(name = "meal_slot")`
    - Fields: id (UUID), planId (UUID, FK → meal_plan), label (String 100), time (String 5), sortOrder (Integer)
    - `@OneToMany(mappedBy = "mealSlotId", cascade = CascadeType.ALL, orphanRemoval = true)` for MealOption

    **MealOption entity** (`MealOption.java`):
    - `@Entity @Table(name = "meal_option")`
    - Fields: id (UUID), mealSlotId (UUID, FK → meal_slot), name (String 100), sortOrder (Integer)
    - `@OneToMany(mappedBy = "optionId", cascade = CascadeType.ALL, orphanRemoval = true)` for MealFood

    **MealFood entity** (`MealFood.java`):
    - `@Entity @Table(name = "meal_food")`
    - Fields: id (UUID), optionId (UUID, FK → meal_option), foodId (UUID, FK → food_catalog, **nullable**, ON DELETE SET NULL — D-10: plan survives catalog deletion), foodName (String 200), qty (String 200), grams (BigDecimal), prep (String 200, nullable), kcal/prot/carb/fat (BigDecimal — frozen macro values per D-05), sortOrder (Integer)

    **PlanExtra entity** (`PlanExtra.java`):
    - `@Entity @Table(name = "plan_extra")`
    - Fields: id (UUID), planId (UUID, FK → meal_plan), name (String 200), quantity (String 200), kcal/prot/carb/fat (BigDecimal), sortOrder (Integer)

    **V5 migration** — Create all tables with:
    - FKs: meal_plan.episode_id → episode(id) UNIQUE, meal_plan.nutritionist_id → nutritionist(id)
    - food_catalog.nutritionist_id → nutritionist(id)
    - meal_food.food_id → food_catalog(id) ON DELETE SET NULL (nullable, D-10)
    - Cascading: meal_slot → meal_option → meal_food (ON DELETE CASCADE)
    - meal_plan → plan_extra (ON DELETE CASCADE)
    - food_catalog → food_portion (ON DELETE CASCADE)
    - Indexes: food_catalog(nutritionist_id), meal_plan(episode_id), meal_plan(nutritionist_id)
    - sort_order columns on meal_slot, meal_option, meal_food, plan_extra, food_portion

    **Repositories** — Create 7 Spring Data JPA repositories:
    - FoodRepository: findByNutritionistId, findByNutritionistIdWithFilters (search + category + pagination)
    - FoodPortionRepository: findByFoodIdOrderBySortOrder
    - MealPlanRepository: findByEpisodeId, findByNutritionistId, findByEpisodeIdAndNutritionistId
    - MealSlotRepository: findByPlanIdOrderBySortOrder
    - MealOptionRepository: findByMealSlotIdOrderBySortOrder
    - MealFoodRepository: findByOptionIdOrderBySortOrder
    - PlanExtraRepository: findByPlanIdOrderBySortOrder

    Follow the exact same @Getter/@Setter pattern from Episode.java. Do NOT use @Data (project convention).
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileJava && ./gradlew test --tests "com.nutriai.api.repository.*"</automated>
  </verify>
  <done>
    - 7 entity classes compile with correct JPA annotations
    - V5 migration creates all tables with correct FKs, cascades, and indexes
    - FoodRepository has nutritionist-scoped query methods
    - MealPlan has UNIQUE constraint on episode_id
    - MealFood.food_id is nullable with ON DELETE SET NULL
    - `./gradlew compileJava` passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Food catalog Service, Controller, DTOs, and tests</name>
  <files>
    backend/src/main/java/com/nutriai/api/service/FoodService.java
    backend/src/main/java/com/nutriai/api/controller/FoodController.java
    backend/src/main/java/com/nutriai/api/dto/food/CreateFoodRequest.java
    backend/src/main/java/com/nutriai/api/dto/food/UpdateFoodRequest.java
    backend/src/main/java/com/nutriai/api/dto/food/FoodResponse.java
    backend/src/main/java/com/nutriai/api/dto/food/FoodPortionDto.java
    backend/src/test/java/com/nutriai/api/service/FoodServiceTest.java
    backend/src/test/java/com/nutriai/api/controller/FoodControllerTest.java
  </files>
  <read_first>
    backend/src/main/java/com/nutriai/api/controller/PatientController.java
    backend/src/main/java/com/nutriai/api/service/PatientService.java
    backend/src/main/java/com/nutriai/api/model/Food.java
    backend/src/main/java/com/nutriai/api/repository/FoodRepository.java
    backend/src/main/java/com/nutriai/api/auth/NutritionistAccess.java
  </read_first>
  <behavior>
    - Test 1: createFood with BASE type stores per100 fields, preset fields null
    - Test 2: createFood with PRESET type stores preset fields, per100 fields null
    - Test 3: listFoods returns only foods belonging to the authenticated nutritionist (data isolation per D-08)
    - Test 4: listFoods with search and category filters works (server-side pagination)
    - Test 5: updateFood modifies the correct fields and persists
    - Test 6: deleteFood removes the food and its portions (cascade)
    - Test 7: Nutritionist A cannot see/modify Nutritionist B's foods (404 returned, not 403)
    - Test 8: FoodController POST /api/v1/foods returns 201 with FoodResponse
    - Test 9: FoodController GET /api/v1/foods returns paginated list with page/size/total
  </behavior>
  <action>
    Build the food catalog backend following Phase 4 PatientController/PatientService patterns exactly:

    **CreateFoodRequest DTO**: type (String, "BASE"/"PRESET"), name (String, @NotBlank), category (String), per100 fields (BigDecimal, nullable, for BASE), preset fields + portionLabel + grams (nullable, for PRESET), portions (List<FoodPortionDto> for BASE foods with name + grams)

    **UpdateFoodRequest DTO**: name, category, per100 fields (nullable), preset fields (nullable), portions (List<FoodPortionDto> — replaces all portions)

    **FoodPortionDto**: name (String), grams (BigDecimal)

    **FoodResponse DTO**: id, type, name, category, per100 fields (nullable), preset fields (nullable), portions (List<FoodPortionDto> with id), usedCount, createdAt, updatedAt

    **FoodService** (following PatientService pattern):
    - `createFood(nutritionistId, request)` — create Food entity, set nutritionistId, save portions if BASE type, return FoodResponse
    - `listFoods(nutritionistId, page, size, search, category)` — repository query with nutritionistId scope, case-insensitive ILIKE search on name, category filter, Pageable with sort by name ASC. Return paginated response with page/size/total/content
    - `getFood(nutritionistId, foodId)` — find by id AND nutritionistId, throw 404 if not found
    - `updateFood(nutritionistId, foodId, request)` — find with scope check, update fields, replace portions list if provided, save
    - `deleteFood(nutritionistId, foodId)` — find with scope check, delete (cascade removes portions)

    **FoodController** (following PatientController pattern):
    - `@RestController @RequestMapping("/api/v1/foods") @PreAuthorize("hasRole('NUTRITIONIST')")`
    - `POST /api/v1/foods` — 201 with FoodResponse, calls `foodService.createFood(NutritionistAccess.getCurrentNutritionistId(), request)`
    - `GET /api/v1/foods?page=0&size=12&search=&category=` — 200 with paginated FoodResponse list
    - `PATCH /api/v1/foods/{id}` — 200 with updated FoodResponse
    - `DELETE /api/v1/foods/{id}` — 204 No Content

    **FoodServiceTest** — unit tests with Mockito:
    - createFood BASE type, createFood PRESET type, listFoods with filters, getFood, updateFood, deleteFood, data isolation (nutritionist A cannot access B's food)

    **FoodControllerTest** — integration tests with @WebMvcTest:
    - POST returns 201, GET returns paginated list, PATCH updates, DELETE returns 204, wrong nutritionist gets 404
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "com.nutriai.api.service.FoodServiceTest" --tests "com.nutriai.api.controller.FoodControllerTest"</automated>
  </verify>
  <done>
    - FoodService CRUD works with nutritionist-scoped data isolation
    - FoodController exposes 4 REST endpoints at /api/v1/foods
    - BASE foods store per100 data + portions; PRESET foods store preset data
    - Server-side pagination with search and category filters
    - All FoodServiceTest and FoodControllerTest tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Meal plan Service, Controller, auto-creation, macro calculation, and tests</name>
  <files>
    backend/src/main/java/com/nutriai/api/service/MealPlanService.java
    backend/src/main/java/com/nutriai/api/controller/PlanController.java
    backend/src/main/java/com/nutriai/api/dto/plan/PlanResponse.java
    backend/src/main/java/com/nutriai/api/dto/plan/AddFoodItemRequest.java
    backend/src/main/java/com/nutriai/api/dto/plan/UpdateFoodItemRequest.java
    backend/src/main/java/com/nutriai/api/dto/plan/AddMealSlotRequest.java
    backend/src/main/java/com/nutriai/api/dto/plan/AddOptionRequest.java
    backend/src/main/java/com/nutriai/api/dto/plan/AddExtraRequest.java
    backend/src/main/java/com/nutriai/api/dto/plan/UpdateExtraRequest.java
    backend/src/main/java/com/nutriai/api/dto/plan/UpdatePlanRequest.java
    backend/src/test/java/com/nutriai/api/service/MealPlanServiceTest.java
    backend/src/test/java/com/nutriai/api/controller/PlanControllerTest.java
  </files>
  <read_first>
    backend/src/main/java/com/nutriai/api/controller/PatientController.java
    backend/src/main/java/com/nutriai/api/service/PatientService.java
    backend/src/main/java/com/nutriai/api/model/Episode.java
    backend/src/main/java/com/nutriai/api/model/MealPlan.java
    backend/src/main/java/com/nutriai/api/model/MealFood.java
    backend/src/main/java/com/nutriai/api/model/Food.java
    backend/src/main/java/com/nutriai/api/repository/MealPlanRepository.java
    backend/src/main/java/com/nutriai/api/repository/EpisodeRepository.java
    backend/src/main/java/com/nutriai/api/auth/NutritionistAccess.java
  </read_first>
  <behavior>
    - Test 1: createDefaultPlan creates plan with 6 meal slots each with 1 empty option
    - Test 2: addFoodItem calculates macros: per100 × grams / 100, stores frozen values
    - Test 3: addFoodItem with PRESET food uses preset grams and nutrition directly
    - Test 4: Editing food in catalog does NOT update existing MealFood frozen macros (D-10)
    - Test 5: getPlan returns full plan tree with meals → options → items + extras
    - Test 6: Plan endpoints return 404 for wrong nutritionist (data isolation)
    - Test 7: updateFoodItem grams triggers macro recalculation from food's per100
    - Test 8: deleteMealSlot cascades to its options and food items
    - Test 9: addExtra and updateExtra and deleteExtra work correctly
    - Test 10: updatePlan changes title/targets/notes
  </behavior>
  <action>
    Build the meal plan service, controller, DTOs, auto-creation logic, and macro calculation:

    **DTOs**:
    - PlanResponse: id, episodeId, title, notes, kcalTarget, protTarget, carbTarget, fatTarget, meals (List<MealSlotResponse>), extras (List<ExtraResponse>), createdAt, updatedAt
    - MealSlotResponse: id, label, time, options (List<MealOptionResponse>)
    - MealOptionResponse: id, name, items (List<MealFoodResponse>)
    - MealFoodResponse: id, foodId (nullable), foodName, qty, grams, prep, kcal, prot, carb, fat
    - ExtraResponse: id, name, quantity, kcal, prot, carb, fat
    - AddFoodItemRequest: foodId (UUID), grams (BigDecimal), qty (String)
    - UpdateFoodItemRequest: grams (BigDecimal, nullable), qty (String, nullable), prep (String, nullable)
    - AddMealSlotRequest: label (String), time (String)
    - AddOptionRequest: name (String)
    - AddExtraRequest: name (String, not null), quantity (String)
    - UpdateExtraRequest: name (nullable), quantity (nullable), kcal/prot/carb/fat (BigDecimal, nullable)
    - UpdatePlanRequest: title (nullable), notes (nullable), kcalTarget/protTarget/carbTarget/fatTarget (BigDecimal, nullable)

    **MealPlanService** — the most complex service. Key methods:
    - `getPlan(nutritionistId, patientId)` — Find active episode for patient (EpisodeRepository.findTopByPatientIdAndEndDateIsNullOrderByStartDateDesc), then find MealPlan by episodeId. Verify nutritionistId ownership via Episode → Patient chain. Return full plan tree (eager-load meals → options → items + extras). Per D-15: plan lives inside PatientView Plans tab.
    - `createDefaultPlan(episodeId, nutritionistId)` — D-13/D-14: Create MealPlan with 6 meal slots: Café da manhã (07:00), Lanche manhã (10:00), Almoço (12:30), Lanche tarde (15:30), Jantar (19:30), Ceia (22:00). Each slot gets 1 empty MealOption named "Opção 1 · Clássico". Default targets: kcal=1800, prot=90, carb=200, fat=60.
    - `updatePlan(nutritionistId, patientId, request)` — Update title, notes, and/or macro targets
    - `addMealSlot(nutritionistId, patientId, request)` — Create MealSlot with given label and time
    - `updateMealSlot(nutritionistId, mealSlotId, label, time)` — D-03: rename/change time
    - `deleteMealSlot(nutritionistId, mealSlotId)` — Remove slot (cascade to options → items)
    - `addOption(nutritionistId, mealSlotId, request)` — D-04: Add named option to meal slot
    - `updateOption(nutritionistId, optionId, name)` — Rename option
    - `deleteOption(nutritionistId, optionId)` — Remove option (cascade to items, only if >1 option remains)
    - `addFoodItem(nutritionistId, optionId, request)` — **D-20: Backend calculates macros.** Look up Food by request.foodId. If BASE: `frozenMacro = food.per100[value] * request.grams / 100`. If PRESET: use `food.presetGrams` as grams and `food.presetXxx` as macros directly. Create MealFood with foodName=food.name, frozen macro values, qty, grams.
    - `updateFoodItem(nutritionistId, itemId, request)` — If grams changed, recalculate macros from food's per100 data (D-20). Update frozen values. If qty or prep changed, just update those fields.
    - `deleteFoodItem(nutritionistId, itemId)` — Remove food from option
    - `addExtra(nutritionistId, patientId, request)` — D-11/D-12: Create PlanExtra with name, quantity, and macros (editable by nutritionist)
    - `updateExtra(nutritionistId, extraId, request)` — Update extra fields
    - `deleteExtra(nutritionistId, extraId)` — Remove extra (no confirmation needed — lightweight action)

    **PlanController** — `@RestController @RequestMapping("/api/v1/patients/{patientId}/plan") @PreAuthorize("hasRole('NUTRITIONIST')")`:
    - `GET /api/v1/patients/{patientId}/plan` → 200 PlanResponse
    - `PATCH /api/v1/patients/{patientId}/plan` → 200 PlanResponse (title, targets, notes)
    - `POST /api/v1/patients/{patientId}/plan/meals` → 201 MealSlotResponse
    - `PATCH /api/v1/patients/{patientId}/plan/meals/{mealId}` → 200 MealSlotResponse
    - `DELETE /api/v1/patients/{patientId}/plan/meals/{mealId}` → 204
    - `POST /api/v1/patients/{patientId}/plan/meals/{mealId}/options` → 201 MealOptionResponse
    - `PATCH /api/v1/patients/{patientId}/plan/meals/{mealId}/options/{optionId}` → 200 MealOptionResponse
    - `DELETE /api/v1/patients/{patientId}/plan/meals/{mealId}/options/{optionId}` → 204
    - `POST /api/v1/patients/{patientId}/plan/meals/{mealId}/options/{optionId}/items` → 201 MealFoodResponse
    - `PATCH /api/v1/patients/{patientId}/plan/meals/{mealId}/options/{optionId}/items/{itemId}` → 200 MealFoodResponse
    - `DELETE /api/v1/patients/{patientId}/plan/meals/{mealId}/options/{optionId}/items/{itemId}` → 204
    - `POST /api/v1/patients/{patientId}/plan/extras` → 201 ExtraResponse
    - `PATCH /api/v1/patients/{patientId}/plan/extras/{extraId}` → 200 ExtraResponse
    - `DELETE /api/v1/patients/{patientId}/plan/extras/{extraId}` → 204

    **IMPORTANT: Wire auto-creation into PatientService.createPatient()** — after creating patient + episode, call `mealPlanService.createDefaultPlan(episode.getId(), nutritionistId)`. Inject MealPlanService into PatientService. This implements D-13/D-14.

    **Macro calculation precision**: Use `BigDecimal` arithmetic with `HALF_UP` rounding, scale=1. Formula: `per100Value.multiply(grams).divide(BigDecimal.valueOf(100), 1, RoundingMode.HALF_UP)`

    **MealPlanServiceTest** — unit tests with Mockito:
    - createDefaultPlan creates 6 slots with 1 option each
    - addFoodItem BASE: calculates macros from per100 × grams / 100
    - addFoodItem PRESET: uses preset grams and preset nutrition
    - updateFoodItem grams change: recalculates macros
    - getPlan returns full tree
    - Data isolation: wrong nutritionist gets 404
    - deleteMealSlot cascades correctly
    - Frozen macros D-10: create plan item, then edit food in catalog, verify plan macros unchanged

    **PlanControllerTest** — integration tests with @WebMvcTest:
    - GET plan returns 200 with full plan structure
    - POST items returns 201 with calculated macros
    - PATCH plan updates targets
    - DELETE meal returns 204
    - Wrong nutritionist gets 404
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "com.nutriai.api.service.MealPlanServiceTest" --tests "com.nutriai.api.controller.PlanControllerTest"</automated>
  </verify>
  <done>
    - MealPlanService creates default 6-meal plan on patient creation (D-13/D-14)
    - Macro calculation: BASE per100 × grams / 100 with BigDecimal precision, PRESET uses preset values directly (D-20)
    - Frozen macros: editing food in catalog does NOT update existing plans (D-10)
    - Full CRUD for meals, options, items, extras (D-01 to D-12, D-16, D-17)
    - Data isolation: nutritionist-scoped with 404 for wrong owner
    - All MealPlanServiceTest and PlanControllerTest tests pass
    - `./gradlew test` passes (all tests across all phases)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted input crosses here (all request DTOs) |
| API → Database | Trusted — but must verify nutritionistId ownership |
| Food catalog → MealFood | Reference by food_id — deletion must not cascade to plan items |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | S | PlanController/FoodController | mitigate | Validate nutritionistId ownership via NutritionistAccess.getCurrentNutritionistId() + repository scope check on every mutation |
| T-05-02 | T | PlanController | mitigate | Patient ID in URL must belong to authenticated nutritionist — verify via Episode → Patient → nutritionistId chain |
| T-05-03 | I | FoodController | mitigate | Food catalog is nutritionist-scoped (D-08) — repository queries filter by nutritionistId |
| T-05-04 | T | MealFood.foodId | mitigate | FK is nullable with ON DELETE SET NULL — prevents cascade deletion of plan items when food removed from catalog |
| T-05-05 | D | PlanController | accept | No rate limiting on auto-save endpoints — acceptable for v1 solo-nutri use; rate limiter in v2 |
| T-05-06 | E | FoodService/MealPlanService | mitigate | All mutation endpoints require @PreAuthorize("hasRole('NUTRITIONIST')") and NutritionistAccess scope check |
</threat_model>

<verification>
1. `./gradlew compileJava` — zero compilation errors
2. `./gradlew test` — all tests pass (existing + new)
3. Flyway V5 migration runs successfully
4. Manual API test: create food → add to plan → verify macros calculated → edit food → verify plan macros unchanged
5. Manual API test: create patient → verify plan auto-created with 6 meals
</verification>

<success_criteria>
- V5 migration creates 7 tables with correct FKs, cascades, and indexes
- Food CRUD API works with nutritionist-scoped data isolation
- Meal Plan CRUD API works with episode linkage and macro calculation
- Auto-creation: creating a patient produces a 6-meal template plan
- Frozen macros: editing food catalog does not affect existing plan items
- All unit and integration tests pass
- `./gradlew test` passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-meal-plans-food-catalog/05-01-SUMMARY.md`
</output>