spring:
  application:
    name: nutriai-api

  datasource:
    url: jdbc:postgresql://localhost:5432/nutriai
    username: nutriai
    password: changeme

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

nutriai:
  version: 0.1.0