spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/nutriai
    username: nutriai
    password: changeme

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    clean-disabled: false

logging:
  level:
    com.nutriai: DEBUG

nutriai:
  jwt:
    cookie:
      secure: false
      same-site: Lax
  seed:
    admin:
      email: admin@nutriai.com
      password: Admin123!
      name: Admin NutriAI